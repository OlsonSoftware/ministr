<!-- Auto-generated by magistr — DO NOT EDIT. Re-run `magistr init` to update. -->
# Tool Guide

## Codebase Navigation (magistr-scope)

| Tool | Purpose |
|------|---------|
| `scope_search` | Primary discovery — semantic + keyword fusion. Start here. |
| `scope_read` | Read files. Use `mode: "stubs"` first, batch with `paths` + `budget`. |
| `scope_grep` | Exact pattern matching. Use after `scope_search` for precision. |
| `scope_imports` | Trace dependencies. Use `transitive: true` before modifying shared code. |
| `scope_modules` | Browse project structure by module. |
| `scope_git` | Git analysis: blame, history, hotspots, evolution, cochange. |

Recommended workflow: `scope_search` → `scope_read` (stubs) → `scope_read` (full) → dig deeper with `scope_grep` / `scope_imports`.

## Quality & Workflow (magistr)

| Tool | Purpose |
|------|---------|
| `magistr_gate` | Run quality gates (`all` / `check` / `test` / `lint` + custom). |
| `magistr_format` | Run the project formatter. |
| `magistr_set_phase` | Announce workflow phase transitions. |
| `magistr_roadmap_status` | Roadmap overview with progress. |
| `magistr_roadmap_tasks` | List tasks with filtering. |
| `magistr_roadmap_check` | Check off / uncheck tasks. |
| `magistr_roadmap_add_task` | Add new tasks. |
| `magistr_roadmap_remove_task` | Remove tasks. |
| `magistr_roadmap_sync` | Regenerate `ROADMAP.md` from canonical data. |
| `magistr_delete` | Delete files/directories (use instead of `rm`). |
| `magistr_list` | List directory contents. |

## Tool Preferences

- Use `scope_search` instead of Glob/find for file discovery.
- Use `scope_grep` instead of Grep/grep for content search.
- Use `scope_read` for exploration; `Read` only immediately before `Edit`.
- Use `magistr_gate` instead of running gate commands manually.
- Use `magistr_roadmap_*` tools instead of manually editing `ROADMAP.md`.
- Use `magistr_delete` instead of `rm` in Bash.
- Do not spawn sub-agents — work directly in the current session.
