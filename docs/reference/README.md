# Reference

- [Tools](tools/README.md) — the MCP tool surface agents use
- [CLI](cli.md) — every `ministr` subcommand and flag
- [Daemon HTTP API](http-api.md) — routes served to local clients
- Configuration keys — see the [configuration guide](../guides/configuration.md)

Pages under `tools/` embed a generated block sourced from the MCP tool manifest
(`cargo run -p ministr-mcp --example gen_tool_docs`) — edit prose outside the
`@generated` markers only.
