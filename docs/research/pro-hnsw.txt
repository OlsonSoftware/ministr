PRO-HNSW: Proactive Repair and Optimization for
   High-Performance Dynamic HNSW Indexes
       Huijun Jin                  Jieun Lee                 Shengmin Piao               Sangmin Seo               Sanghyun Park†
   Computer Science         Computer Science      Artificial Intelligence Computer Science     Computer Science
    Yonsei University        Yonsei University      Yonsei University      Yonsei University    Yonsei University
      Seoul, Korea             Seoul, Korea           Seoul, Korea           Seoul, Korea         Seoul, Korea
 jinhuijun@yonsei.ac.kr jieun199624@yonsei.ac.kr shengminp@yonsei.ac.kr ssm6410@yonsei.ac.kr sanghyun@yonsei.ac.kr



   Abstract—Graph-based Approximate Nearest Neighbor Search
(ANNS), particularly using Hierarchical Navigable Small World
(HNSW) graphs, offers state-of-the-art query performance for
large-scale, high-dimensional data. However, the efficiency and
accuracy of HNSW indexes degrade seriously under dynamic
conditions involving frequent data deletions and reinsertions, of-
ten necessitating costly full index rebuilds. This paper introduces
PRO-HNSW (Proactive Repair and Optimized HNSW), a novel
and lightweight maintenance framework designed to efficiently
preserve and even enhance the quality of the HNSW graph in
dynamic environments. PRO-HNSW systematically addresses key
structural degradation factors by incorporating three targeted
modules: (1) a RemoveObsoleteEdges module to prune edges
pointing to deleted nodes while preserving minimal connectivity,         Fig. 1. Maximum recall drop during consecutive update scenarios.
(2) a RepairDisconnectedNodes module that re-establishes
connectivity for isolated nodes via BFS-based neighbor discov-
ery, and (3) a ResolveUnidirectionalEdges module to
ensure more balanced and navigable graph structures. Extensive        long-range edges at upper layers facilitating fast traversal and
experiments on eight diverse benchmark datasets demonstrate           short-range edges at lower layers enabling precise navigation.
that PRO-HNSW significantly outperforms not only the standard            Despite its efficacy for static datasets, the performance
HNSW but also the state-of-the-art MN-RU method. Notably, on          of HNSW indexes can seriously degrade in dynamic envi-
the high-dimensional GIST1M dataset under high data churn,
PRO-HNSW achieves a peak recall of 84.23%, a significant 2.56         ronments where nodes are frequently deleted and inserted.
percentage points higher than even the ideal HNSW-Rebuild             Real-world systems often deal with constantly evolving data,
baseline, showcasing its superior ability to produce a more           making efficient and effective update mechanisms crucial for
optimal graph structure. Our findings also reveal that applying       maintaining search performance over time. For instance, in
specific PRO-HNSW maintenance modules to a freshly built static       a large-scale e-commerce platform, product catalogs are con-
HNSW index can improve its initial recall, highlighting its utility
as a general graph optimization technique. PRO-HNSW thus              tinuously updated with new items being added and seasonal
provides a practical and robust solution for maintaining high-        or out-of-stock items being removed [2], [4]. While HNSW
performance ANNS in evolving real-world applications.                 supports deletions by marking nodes as deleted and reinserting
   Index Terms—Approximate Nearest Neighbor Search, Hierar-           new nodes into these vacated slots, this process often leads to
chical Navigable Small World, Update Performance Optimiza-            a suboptimal graph structure over time. The cumulative effect
tion, Lightweight Graph Repair
                                                                      of such updates can result in increased query latency, reduced
                                                                      recall, and an overall decline in search quality.
                       I. I NTRODUCTION                                  To quantify the impact of sustained updates on search
   Approximate Nearest Neighbor Search (ANNS) is a funda-             accuracy, we evaluated the maximum recall drop for standard
mental operation in many applications, including recommen-            HNSW in a challenging consecutive update scenario across our
dation systems [1]–[3], image retrieval [4], [5], and natural         eight benchmark datasets. In this experiment, for each dataset,
language processing [6]–[8]. Among various ANNS algo-                 we simulated a total data turnover of 50% by performing 100
rithms, Hierarchical Navigable Small World (HNSW) graphs              consecutive update iterations, where each iteration involved
have gained significant popularity due to their excellent search      randomly deleting and reinserting a distinct 0.5% of the base
performance, achieving a remarkable balance between speed             vectors. As illustrated in Fig. 1, the performance degradation
and accuracy across diverse datasets [9]. HNSW constructs a           under this sustained workload can be severe, with observed
multi-layer graph where nodes are progressively added, with           maximum recall drops reaching 14.88% for DeepImage and
                                                                      14.71% for SIFT1M. This considerable degradation highlights
  † Corresponding Author                                              the inherent vulnerability of the standard HNSW approach.
The conventional remedy, a full index reconstruction, is com-                   II. BACKGROUND AND R ELATED W ORK
putationally prohibitive for many real-world scenarios char-          A. Approximate Nearest Neighbor Search
acterized by high update throughput and stringent uptime
requirements. This challenge underscores the pressing need               The goal of a nearest neighbor search is to find the data
for efficient and lightweight maintenance strategies for HNSW         points in a dataset X that are closest to a given query point
under dynamic workloads.                                              q. While straightforward for low-dimensional data, finding the
                                                                      exact nearest neighbors becomes computationally prohibitive
   The primary sources of this performance degradation in             in high-dimensional spaces—a phenomenon widely known as
updated HNSW graphs, as our investigation reveals, include:           the “curse of dimensionality” [12]–[14]. To overcome this
(1) the accumulation of obsolete edges pointing to marked-            challenge, Approximate Nearest Neighbor Search (ANNS)
deleted nodes, which misguide search; (2) the emergence of            was proposed as a practical alternative. The core idea of
disconnected nodes, resulting parts of the graph unreachable;         ANNS is to trade a small, acceptable amount of accuracy for a
and (3) the proliferation of unidirectional edges, which disrupt      massive gain in search speed. This trade-off is highly effective
balanced graph traversal. While some existing approaches              for many real-world applications where slight imperfections
attempt to mitigate these issues through periodic localized re-       are tolerable, such as recommendation systems [1], [2], image
building [10] or basic heuristic repairs [9], [11], they often fall   retrieval [4], [5], and natural language processing [6], [7].
short of comprehensively addressing the multifaceted nature              The trade-off between search accuracy (recall) and effi-
of graph quality degradation or impose significant overhead           ciency (queries per second, QPS) is a central theme in ANNS
themselves.                                                           research. Various ANNS algorithms have been proposed,
                                                                      broadly categorized into tree-based [15]–[18], hashing-based
   To overcome these limitations, this paper introduces PRO-          [19]–[22], quantization-based [13], and graph-based methods
HNSW (Proactive Repair and Optimized HNSW), a novel                   [9], [23]–[25]. Among these, graph-based ANNS algorithms
and lightweight maintenance framework specifically designed           have recently demonstrated state-of-the-art performance on
to proactively and efficiently repair and even enhance the qual-      many benchmark datasets [26], [27]. Their success stems
ity of HNSW graphs after a series of updates. Instead of re-          from their ability to effectively capture complex neighborhood
sorting to costly rebuilds or overly simplistic heuristics, PRO-      structures in high-dimensional data, enabling efficient greedy
HNSW employs a suite of targeted, low-overhead algorithmic            search traversals that balance global exploration and local
modules. These modules systematically address the identi-             refinement. Within this category, HNSW has become one of
fied root causes of degradation: a RemoveObsoleteEdges                the most popular and widely adopted methods due to its
module tackles obsolete edges while preserving minimal                superior accuracy-speed trade-off.
connectivity; a RepairDisconnectedNodes module re-
establishes crucial connectivity for isolated nodes; and a            B. Hierarchical Navigable Small World Graphs
ResolveUnidirectionalEdges module ensures more
                                                                         HNSW [9] is a graph-based ANNS algorithm known for its
balanced graph structures. Our contributions are:
                                                                      excellent search performance. It constructs a multi-layer graph
                                                                      where each layer is a Navigable Small World (NSW) graph
  • We propose PRO-HNSW, a novel and lightweight main-                [28], enabling both fast traversal and precise navigation.
    tenance framework for HNSW indexes that efficiently                  Hierarchical Structure. The HNSW graph is built by
    recovers and sustains graph quality under various update          inserting nodes one by one. Each new node is assigned a
    scenarios, offering a significantly more cost-effective           maximum level, lmax , chosen randomly from an exponentially
    alternative to full index reconstructions.                        decaying probability distribution. This approach creates a
  • We systematically identify and analyze the root causes            hierarchical structure: the bottom layer (level 0) is a dense
    of HNSW graph quality degradation under dynamic up-               graph containing all nodes, connected by short-range edges
    dates—namely, obsolete edges, disconnected nodes, and             for fine-grained search, while upper layers are progressively
    unidirectional edges—and present targeted, low-overhead           sparser, featuring long-range “highway” edges for efficient,
    algorithmic solutions integral to PRO-HNSW, for each              coarse-grained traversal across the vector space.
    identified cause.                                                    Construction. During construction, the algorithm traverses
  • We conduct extensive experiments demonstrating that               from the top layer downwards. At each level, it finds
    PRO-HNSW significantly outperforms both standard                  approximate neighbors for the new node, guided by the
    HNSW and state-of-the-art MN-RU variants. We show                 ef Construction (efc ) parameter, which controls the trade-
    that PRO-HNSW not only achieves the highest peak                  off between build time and graph quality. A crucial step is
    recall in all scenarios but also surpasses the theoretical        the heuristic-based neighbor selection, which selects up to M
    optimum of the HNSW-Rebuild baseline.                             neighbors for upper layers (l > 0) and M0 (typically 2M )
  • We validate the fundamental soundness of our repair               for the base layer, balancing proximity and diversity to ensure
    modules by showing they can improve search recall even            robust navigability.
    on static graphs, emphasizing their versatility as a general         Search. A search in HNSW also starts at the top layer,
    graph optimization approach.                                      greedily traversing edges to find the node closest to the query
at each level before descending. This process is repeated until     edge u → v might be established because v is selected as one
the search reaches level 0, where a final, more refined search is   of the best neighbors for u, but the reverse is not guaranteed, as
performed. The trade-off between search speed and accuracy          u may not be among the best neighbors from v’s perspective.
is controlled at query time by the ef Search (efs ) parameter,      While this level of asymmetry is inherent to HNSW, dynamic
which dictates the size of the dynamic candidate list.              updates can significantly exacerbate the issue by breaking
   Update. For updates, standard implementations employ a           existing symmetric pairs or creating new one-way connections
mark-and-replace mechanism: a node is flagged as deleted,           during reinsertions into a structurally altered neighborhood.
and its slot is later reused for a new insertion. While simple,        These unidirectional edges effectively create “one-way
this approach is the source of the structural degradation issues    streets” in the graph’s navigational structure, which can
that motivate our work.                                             severely disrupt the greedy search process. If a search traverses
                                                                    an edge from node u to v but cannot return or explore adjacent
C. Challenges in Maintaining High-Performance HNSW                  areas due to a missing reciprocal edge, it can get trapped in
   In dynamic environments, the continuous churn of insertions      a suboptimal region of the graph. Unable to backtrack to a
and deletions systematically damages the structural quality         more promising path, the search is often forced to converge
of the HNSW graph. This degradation directly compromises            on a less accurate result, ultimately failing to discover the true
search efficiency, making robust maintenance strategies essen-      nearest neighbors and thereby lowering recall.
tial for real-world applications. However, the standard HNSW           Addressing these challenges without resorting to frequent
approach presents several critical challenges that progressively    and costly full index rebuilds is the primary motivation for
degrade the index:                                                  our work. Our proposed lightweight maintenance modules are
   Obsolete Edges. When a node u is marked for deletion             designed to proactively and efficiently counteract these specific
in HNSW, its entry in the graph is typically only flagged,          degradation factors, thereby preserving and even enhancing the
while other alive nodes v that previously established an edge       search performance of HNSW indexes in dynamic settings.
v → u often retain this edge. These lingering connections
become obsolete or “dead” edges. As updates accumulate, the         D. Related Work on Updatable Graph-Based ANN Indexes
graph becomes increasingly littered with them, leading to two          Graph-based methods for ANNS have shown considerable
primary negative consequences. First, during search, traversing     success in providing efficient and accurate search results for
these obsolete edges results in wasted computations and can         high-dimensional data [27]. Algorithms such as HNSW [9],
misguide the search trajectory, which directly increases query      NSG [24], Vamana (used in DiskANN [25]), and others
latency and harms recall. Second, their accumulation unnec-         [23], [28]–[35] construct graph structures where nodes rep-
essarily inflates the out-degree of alive nodes, which damages      resent data points and edges represent proximity, enabling
the effectiveness of neighbor selection heuristics during subse-    efficient greedy-search traversals. While these methods excel
quent insertions and graph maintenance by cluttering neighbor       in static settings, their performance can significantly degrade
lists with useless connections.                                     in dynamic environments where data is frequently inserted or
   Disconnected Nodes. Node deletions and reinsertions can          deleted, a common scenario in real-world applications. The
cause more severe structural damage than just obsolete edges:       primary challenge lies in maintaining the graph’s structural
parts of the graph can become entirely disconnected from            integrity and search efficiency without resorting to costly full
the main structure. A node might lose all of its incoming           index rebuilds.
edges, especially at the critical upper “highway” layers of            Addressing updates in disk-based graph ANN indexes, Yu
HNSW. This happens when the few neighbors pointing to it            et al. [36] highlight significant challenges related to I/O over-
are deleted or have their own connections updated. Such a           head and inefficient neighbor pruning, particularly with small-
node, now lacking any incoming paths, effectively becomes           batch updates. Their system, Greator, proposes a lightweight
an “unreachable island” in the graph. During a search, there        topology for faster affected-vertex identification and localized
is no path from the main entry points to this “island.” As          page-level updates to minimize I/O. It also introduces a “sim-
a result, the isolated node itself, along with any other nodes      ilar neighbor replacement” method to reduce costly pruning
exclusively connected to it, becomes effectively invisible to       operations. While Greator’s emphasis on localized repairs to
the search algorithm [11].                                          mitigate update overheads shares a conceptual similarity with
   This problem can escalate to graph fragmentation, where          PRO-HNSW, its solutions are specifically tailored for disk
the entire graph shatters into one large “continent” and multi-     I/O efficiency and persistence, whereas PRO-HNSW focuses
ple smaller, isolated “islands”. Consequently, if a query’s true    on lightweight computational fixes for in-memory HNSW
nearest neighbors happen to reside on one of these “islands”,       structures.
they become entirely inaccessible, leading to a drastic and            For in-memory HNSW, Xiao et al. [11] investigate the “un-
unpredictable drop in recall [11]. This issue is particularly       reachable points phenomenon” and performance degradation
acute because the success of HNSW’s search fundamentally            stemming from standard HNSW update mechanisms. Their
relies on the ability to navigate the entire graph seamlessly.      MN-RU algorithm aims to mitigate these issues by redesigning
   Unidirectional Edges. The HNSW construction heuristic            deletion and update strategies, including specific neighbor
does not strictly guarantee that edges are bidirectional. An        connection repairs. A notable aspect of their approach is
                                                                    Algorithm 1: RemoveObsoleteEdges
                                                                     Input: A set of deleted nodes delSet, alive nodes U , a
                                                                             threshold T
                                                                   1 for alive node u ∈ U do
                                                                   2     for l ← 0 to Lu do
                                                                   3         N(u,l) ← getN eighbors(u, l)
                                                                   4         can ← countAliveN eighbors(N(u,l) , delSet)
                                                                   5         if can < T then
         Fig. 2. Remove or reserve obsolete edges (M = 2).
                                                                   6             continue
                                                                                ′
                                                                    7         N(u,l) ← ∅ // initialize a new list
                                                                    8         for n ∈ N(u,l) do
the introduction of a dual-index architecture, comprising a
                                                                    9             if n ∈
                                                                                       / delSet then
main HNSW index and an auxiliary “HNSW Backup In-                                       ′
                                                                   10                 N(u,l) .add(n)
dex” designed to manage and retrieve otherwise unreachable
points. Queries are then performed across both indexes (“du-       11
                                                                                                      ′
                                                                              updateN eighbors(u, l, N(u,l) )
alSearch”). While this dual-index strategy can help in finding
points that become isolated in the main index, it inherently
incurs additional memory overhead for storing and maintaining
the secondary index, and potentially adds complexity to the
                                                                   which re-establishes connectivity for nodes that may have be-
query process and overall system management.
                                                                   come isolated; and (3) ResolveUnidirectionalEdge,
   In contrast, our PRO-HNSW framework offers a fundamen-
                                                                   which ensures that critical navigational edges are bidirectional
tally different approach. Instead of introducing auxiliary data
                                                                   or otherwise balanced.
structures like the “Backup Index” in MN-RU, it operates
with a suite of lightweight, in-place modification modules         A. Remove Obsolete Edges (ROE)
that directly enhance the structural integrity of the single,
primary HNSW graph. This design not only addresses node               Conceptual Overview. The ROE module is designed to
unreachability but also systematically resolves distinct issues    prune obsolete edges that point to deleted nodes. Fig. 2
of obsolete edges and unidirectional edges through compu-          conceptually illustrates the scenarios ROE handles and the
tationally efficient repairs. Our findings also underscore that    core idea behind its heuristic. Initially, when a node (say
PRO-HNSW serves as a foundational enhancement to the               v2 in Fig. 2a) is marked for deletion, its outgoing edges are
core HNSW framework for both dynamic and static scenarios.         conceptually removed, but incoming edges from alive nodes
This corroborates a similar principle observed by Zardbani et      (e.g., from v3 , v5 , highlighted in red) might persist. If the slot
al. [37] in the context of spatial indexing, where a method        of v2 is later reused by a new vector, these persistent incoming
designed for adaptive updates also proved highly effective for     edges become obsolete, now pointing to the updated node v2′
building an efficient static index.                                (Fig. 2b). Our ROE module targets such obsolete connections.
   Furthermore, we believe the core principles of PRO-             As shown in Fig. 2c, obsolete edges (e.g., from v3 , v5 to v2′ )
HNSW—proactively pruning obsolete edges (ROE), repairing           are pruned if their source nodes possess other outgoing edges,
graph connectivity for isolated nodes (RDN), and ensuring          thereby maintaining their connectivity. However, a crucial
edge reciprocity (RUE)—address fundamental challenges in-          safeguard is incorporated: Fig. 2d depicts a situation where
herent to any dynamic graph-based ANNS method, not just            pruning an obsolete edge (e.g., from v3 or v5 to a deleted
HNSW. For instance, popular algorithms such as NSG and             v4 ) would leave the source node (v3 or v5 ) with no outgoing
Vamana, which also rely on greedy-search traversals, face          connections. In such cases, ROE preserves this last outgoing
similar structural degradation from frequent updates. While        edge, even if obsolete, to prevent node isolation.
the direct application and evaluation of our modules on these         ROE Algorithm. The detailed procedure is outlined in
alternative graph structures are beyond the scope of this paper,   Algorithm 1. The algorithm iterates through all alive nodes
it presents a compelling avenue for future work. Our current       U and all their levels L, assuming that a set of deleted nodes
research focuses exclusively on validating the effectiveness of    denotes delSet. The neighbor list N(u,l) of the alive node
these principles within the HNSW framework.                        u on the level l is first obtained (Line 3), and the number
                                                                   of its “alive” neighbors can (i.e., those not in the delSet) is
            III. T HE PRO-HNSW F RAMEWORK                          counted using the countAliveN eighbors function (Lines 4).
   The PRO-HNSW is designed to be lightweight and is               A predefined threshold T , typically set to 1, denotes the
consists of three core algorithmic modules, each targeting a       minimum number of alive neighbors that a node should retain
specific type of structural degradation commonly observed          at any level. If can is less than T , the neighbor list N(u,l)
after data deletions and reinsertions. These modules are: (1)      is left unchanged for that node and level to preserve minimal
RemoveObsoleteEdges, which prunes outdated connec-                 connectivity (Lines 5 to 7). Otherwise, if can meets or exceeds
                                                                                      ′
tions to deleted nodes; (2) RepairDisconnectedNodes,               T , a new list N(u,l)    is constructed containing only the alive
                                                                       Algorithm 2: RepairDisconnectedNodes
                                                                   1 allComps ← f indAllComponents()
                                                                   2 ctn ← countAliveN odes()
                                                                   3 Tc ← ctn / 2
                                                                   4 for comp ∈ allComps do
                                                                   5     if sizeOf(comp) < Tc then
                                                                   6          for u ∈ comp and u ∈ / delSet do
                                                                   7              Lu ← getM axLevel(u)
                                                                   8              for l ← 0 to Lu do
                                                                   9                  limitedHopRepair(u, l)
     Fig. 3. Node deletion and repair disconnected node (M = 2).



neighbors (i.e., n ∈ N(u,l) such that n is NOT IN delSet)
                                                                     Algorithm 3: limitedHopRepair(ut , lt )
(Lines 8 to 10). Finally, the neighbor list of u at level l
                   ′
is updated to N(u,l)    via updateN eighbors (Line 11). This          Input: A target node ut to be repaired, a level lt
selective pruning mechanism ensures the graph is cleansed           1 visited ← newSet(); visited.add(ut )

of unhelpful obsolete edges while cautiously preventing node        2 f rontier ← newQueue(); f rontier.enqueue(ut )

isolation, typically after a batch of nodes has been marked for     3 hop ← 0

deletion.                                                           4 while f rontier ̸= ∅ do

   Time Complexity. The time complexity of Algorithm 1              5     hop ← hop + 1
is primarily determined by iterating through all alive nodes        6     isAddedInT hisHop ← false
and their neighbors across all levels. Let N be the number of       7     nextF ← ∅ // init a next frontier
alive nodes in the graph, Lavg be the average maximum level         8     for u ∈ f rontier do
of a node, and Mavg be the average number of neighbors              9         N(u,lt ) ← getN eighbors(u, lt )
per node per level. The algorithm accesses each neighbor list      10         for n ∈ N(u,lt ) do
and checks each neighbor against the delSet. Thus, the total       11             visited.add(n)
number of such checks, which constitutes the dominant part of      12             nextF.add(n)
the computation, can be approximated as O(N · Lavg · Mavg ).       13             if addIntoEmptySlot(n, ut , lt ) then
The check against delSet (an unordered set) takes, on average,     14                  isAddedInT hisHop ← true
O(1) time per neighbor.
                                                                   15     if hop ≥ 3 and isAddedInT hisHop then
B. Repair Disconnected Nodes (RDN)                                 16         break
   Conceptual Overview. The RDN module addresses the               17     f rontier ← nextF
issue of graph fragmentation by identifying and re-establishing
connectivity for isolated nodes. Fig. 3a depicts an example
graph segment. If a node v5 is subsequently deleted (Fig. 3b),
v1 loses its only remaining incoming connection, thereby           levels originally populated by u (up to Lu , Lines 7 to 8).
becoming isolated. To rectify such situations, the RDN module      Subsequently, it invokes the limitedHopRepair procedure
is applied. Fig. 3c shows the outcome: the RDN procedure,          (Line 9) to establish up to this target number of connections
by invoking the limitedHopRepair subroutine (Algorithm 3),         for node u at that level.
attempts to establish new incoming connections for v1 . In            The core repair mechanism is encapsulated within the
this illustrative example, a new incoming edge v4 → v1 is          limitedHopRepair subroutine, as shown in Algorithm 3. This
successfully added, reintegrating v1 into the navigable graph      procedure aims to establish new incoming links to the target
structure.                                                         node ut at the specified level lt by performing a limited-hop
   RDN Algorithm. The Algorithm 2 formalizes this repair           BFS. Starting from ut (Lines 1 to 2), the BFS explores nodes
process. It begins by invoking a f indAllComponents func-          in its f rontier (Lines 8 to 17). For each valid, unvisited, and
tion (Line 1), which identifies all distinct connected compo-      undeleted neighbor n discovered (by iterating N(u,lt ) obtained
nents (allComps) within the graph. After determining the           via getN eighbors), an attempt is made to add an incoming
total number of alive nodes, ctn (Line 2), a size threshold,       edge from n to ut at lt using the addIntoEmptySlot function
Tc , is established as ctn /2 (Line 3). This threshold is based    Lines 8 to 13). This function adds the edge only if n has an
on the observation that fragmented graphs typically consist        available slot at lt . This strategy of targeting only available
of one main component and smaller outliers; thus, Tc targets       slots is designed to minimize computational overhead by
these smaller “fragment” components for repair. For a node         avoiding costly heuristic-based neighbor selection when a
u within a small component (comp) that is NOT IN the               simple addition suffices, while still effectively establishing new
delSet (Lines 4 to 6), the algorithm iterates through all          links to enhance the connectivity of the ut node. The BFS
                                                                           Algorithm 4: ResolveUnidirectionalEdges
                                                                       1 for alive node u in the graph do
                                                                       2     N(u,l0 ) ← getN eighbors(u, l0 )
                                                                       3     for n ∈ N(u,l0 ) do
                                                                       4         N(n,l0 ) ← getN eighbors(n, l0 )
                                                                       5         isReciprocated ← false
                                                                       6         for v ∈ N(n,l0 ) do
                                                                       7              if v = u then
    Fig. 4. Node insertion and resolve unidirectional edges (M = 2).
                                                                       8                  isReciprocated ← true
                                                                       9                  break
                                                                       10        if not isReciprocated then
proceeds for a limited number of hops (e.g., up to 3 hops),
                                                                       11            attemptAddIncomingEdge(n, u, l0 )
terminating early if a new link is added in a hop after an initial
exploration phase. This multi-level repair strategy is crucial
for repairing not just local connectivity at level 0 but also the
long-range navigational links at upper layers that are vital for        Algorithm 5: attemptAddIncomingEdge(us , ut , l)
HNSW’s search efficiency.
                                                                         Input: A source node us , a target node ut , a level l
   Time Complexity. The RDN procedure involves two main                1 N(ut ,l) ← getN eighbors(ut , l)
computational stages. First, the f indAllComponents step,              2 cmn ← getM axN eighbors(l)
typically implemented using BFS across all graph layers, has           3 if sizeOf(N(ut ,l) ) < cmn then
a time complexity of approximately O(N + E), where N                   4      N(ut ,l) .add(us )
is the total number of nodes and E is the total number                 5      return true
of edges in the graph. Second, for each node u identified
within a “small” component (let Ns be the total count of such          6  else
nodes), the algorithm iterates up to its maximum original level        7      candidateSet ← N(ut ,l) ∪ us
Lu (with an average maximum level of Lavg across these                  8     vect ← getDataV ector(ut )
nodes) and invokes the limitedHopRepair subroutine. The                 9     Ns ← selectN eighbors(candidateSet, vect , cmn )
limitedHopRepair performs a BFS up to a small, constant                10     Update N(ut ,l) with Ns
number of hops k. In each hop, it explores up to Mavg
neighbors, and for each of these, it again explores Mavg
neighbors. The dominant operation within this BFS is often             arises when node v1 ’s neighbor list is already full, or the
the neighbor retrieval and checks. If we consider the number           neighbor selection heuristic does not consider v3 a better
                                                              k
of nodes visited in k hops to be roughly on the order of Mavg          candidate than its existing connections. This pattern continues
in sparse regions, and for each, Mavg neighbors are processed,         in Fig. 4d with the insertion of v4 , where the edge v4 → v3
the complexity of a single limitedHopRepair call might be              lacks its reciprocal v3 → v4 . Fig. 4e illustrates the outcome
                        k+1
approximated as O(Mavg      ). Thus, the total complexity for the      after applying our RUE module. The procedure identifies such
                                                      k+1
repair phase can be estimated as O(Ns · Lavg · Mavg       ). The       unidirectional edges (e.g., involving v3 and v4 ) and attempts to
overall complexity of RDN will be dominated by either the              establish the missing reciprocal links, for instance, by adding
component finding or the cumulative repair phase, depending            the edge v3 → v4 (shown in red). The outcome is a graph with
on the graph’s state and the number of small components.               resolved unidirectional edges, leading to a more balanced and
                                                                       robustly navigable structure.
C. Resolve Unidirectional Edges (RUE)
                                                                          RUE Algorithm. The Algorithm 4 formalizes this resolving
   Conceptual Overview. The RUE module is designed to                  process. Our approach deliberately focuses this procedure on
resolve unidirectional edges by attempting to establish recip-         level 0. This is because level 0 is the only layer guaranteed
rocal connections where they are missing. Fig. 4 provides              to contain all nodes and is the densest layer where the
a step-by-step illustration of how unidirectional edges can            vast majority of unidirectional edges occur, making it the
arise during HNSW construction and how our RUE module.                 most impactful layer for resolving unidirectional edges while
Initially, Fig. 4a depicts the insertion of the first node, v1 .       avoiding the significant computational overhead of processing
Subsequently, as shown in Fig. 4b, node v2 is inserted, and            all layers. The Algorithm 4 iterates through all alive nodes in
ideal bidirectional edges v1 ↔ v2 (newly formed edges are              the graph. For each outgoing neighbor n of the alive node u
highlighted with red dashed lines) are established. However,           found in its level 0 neighbor list, N(u,l0 ) (Line 2), it inspects
unidirectional edges can emerge as more nodes are added.               n’s level 0 neighbor list, N(n,l0 ) (Line 4), to determine if u
In Fig. 4c, node v3 is inserted. While new edges v3 → v1 ,             is present. The boolean variable isReciprocated tracks this
v3 → v2 , and v2 → v3 are formed, a reciprocal edge v1 → v3            (Lines 5 to 9). If this reciprocal edge is missing, the procedure
is notably absent. This type of unidirectional edges typically         calls attemptAddIncomingEdge (Line 13) to potentially add
                                                                   TABLE I
                       DATASET S TATISTICS , DATA T YPES , D ISTANCE M ETRICS , AND T IER -S PECIFIC HNSW PARAMETERS .

      Dataset                 Size     Dim.      # Vec.    # Qry.      Data Type        Metric    Size Tier   M     efc   efre    efs
      DeepImage [38]        3.6 GB      96    9,990,000    10,000    Image Features     Cosine
                                                                                                   Large      16   100    75     75-125
      GIST1M [13]           3.6 GB     960    1,000,000     1,000    Image Features      L2
      SIFT1M [13]           501 MB     128    1,000,000    10,000     Image Features     L2
                                                                                                  Medium      12    75    50     50-100
      NYTimes [39]          301 MB     256      290,000    10,000    Doc Embeddings     Cosine
      MNIST [40]            217 MB     784       60,000    10,000    Image Features      L2
      Fashion-MNIST [41]    217 MB     784       60,000    10,000    Image Features      L2
                                                                                                   Small      8     50    25     25-75
      COCO-I2I [42]         136 MB     512      113,287    10,000    Image Features     Cosine
      GloVe-25 [43]         122 MB      25    1,183,514    10,000   Word Embeddings     Cosine



u to n’s neighbor list at level 0.                                      based selectN eighbors heuristic. However, in the dynamic
   The attemptAddIncomingEdge subroutine, detailed in                   settings we address, frequent deletions naturally create open
Algorithm 5, is responsible for the actual edge balancing.              slots in neighbor lists, making the far more common operation
It first retrieves the current neighbor list N(ut ,l) for the           a simple, low-cost edge insertion. This is empirically validated
node ut at level l (Line 1) and determines the maximum                  by our experimental results in Section IV-E (Table III), which
allowed neighbors, cmn , for the level (Line 2). If N(ut ,l)            demonstrate that the practical, normal-case overhead of the
has fewer than cmn entries (Line 3), us is directly added to            RUE module is minimal.
ut ’s neighbor list, and the subroutine returns true. However,
                                                                                                 IV. E VALUATION
if N(ut ,l) is full (Line 6), a heuristic selection is triggered.
All existing neighbors in N(ut ,l) plus us form a candidate                In this section, we present a comprehensive experimental
set candidateSet (Line 7). The data vector vect is retrieved            evaluation of PRO-HNSW. We aim to demonstrate its effec-
(Line 8), and a selectN eighbors function (Line 9) is invoked.          tiveness in maintaining high search performance under various
This heuristic, mirroring HNSW’s neighbor selection, chooses            update scenarios compared to standard HNSW and state-of-
the best cmn neighbors. The neighbor list of ut , N(ut ,l) , is         the-art methods.
then updated (Line 10). This overall process aims to resolve
                                                                        A. Experimental Setup
unidirectional edges, thereby enhancing graph navigability.
   Time Complexity. The RUE algorithm primarily operates                   Environment. All experiments were conducted on a server
on level 0 of the HNSW graph. It iterates through all alive             equipped with a 12th Gen Intel(R) Core(TM) i7-12700F CPU
nodes (let there be N such nodes) and, for each of their                (2.50GHz base) and 94GB of RAM, running Ubuntu 20.04
M0 neighbors n, it again checks n’s M0 neighbors to find                LTS.
u. This check for a reciprocal edge takes O(M0 ) time. If an               Metrics. The primary metrics for evaluating search perfor-
unidirectional edge is found, the attemptAddIncomingEdge                mance are:
subroutine is called. In the best-case scenario where n has an             • Recall@K: We use Recall@10, defined as the proportion

available slot, adding the edge is typically O(1) after checking              of queries for which at least one of the true top-10 nearest
existing neighbors (O(M0 )). However, if n’s neighbor list is                 neighbors is found within the top-10 returned results.
full, the heuristic neighbor selection process selectN eighbors            • Queries Per Second (QPS): Measures search throughput,

is invoked. This heuristic usually involves calculating distances             averaged of 3 runs using a single query.
from n to M0 + 1 candidate neighbors and then selecting                    Datasets. We evaluated PRO-HNSW on eight publicly
the best M0 . If distance calculations take O(d) (where d is            available benchmark datasets, which are standard in ANNS
the vector dimensionality) and selection involves a sort or             research. These datasets, detailed in Table I, cover a diverse
priority queue operations, this step might be around O(M0 ·             range of dimensionalities, sizes, data types, and distance
d + M0 log M0 ). Therefore, the overall time complexity of the          metrics. Specifically, we categorize these datasets into three
RUE module can be approximated as O(N ·M02 ·(d+log M0 ))                tiers based on their approximate file size: Large, Medium, and
in the more complex case involving the heuristic. Given its             Small. The ground truth nearest neighbors for all query sets
operation primarily on the dense level 0, the number of edges           were used as provided with the datasets.
considered can be substantial. To manage this computational                Parameters. The HNSW parameters were selected using
load effectively, our implementation of the RUE module                  a tier-based configuration tailored to dataset characteristics.
is designed to leverage parallel processing across multiple             These include the core construction parameters: M , which
threads, thereby significantly reducing its effective execution         defines the maximum number of outgoing edges each node can
time. While the theoretical worst-case complexity appears               have, and efc (ef Construction), which controls the size of
high, this scenario is rarely triggered in practice. The worst          the dynamic candidate list during index building to determine
case occurs when adding a reciprocal edge to a node whose               graph quality. We also define two dynamic parameters: efre ,
neighbor list is full, thereby invoking the costly distance-            which serves as the ef Construction value during the element
                                Fig. 5. QPS-Recall@10 comparison across datasets in bulk update scenario.



reinsertion phase, and efs , a query-time parameter that also         ing dynamic update scenarios. The results are presented in
controls the candidate list size to balance search speed and          Fig. 5 and Fig. 6. Within each subplot, we illustrate the
recall. Specific values for these parameters for each tier are        QPS-Recall@10 performance using Pareto frontier lines for
detailed in Table I. For all experiments, unless otherwise            multiple conditions: four baselines with each at two distinct
noted, these tier-specific parameters were consistently applied       deletion ratios, δ = 0.2 (small churn) and δ = 0.8 (large
to both HNSW and PRO-HNSW for fair comparison. The efs                churn).
parameter was varied across the specified range for each tier            Bulk Updates. We first evaluated all methods under a
in unit increments to generate Recall-QPS trade-off curves.           bulk update scenario, which simulates large, instantaneous
   Baselines. To rigorously evaluate the performance of our           data churn. For each dataset, a significant portion—δ = 0.2
proposed PRO-HNSW, we compare it against four distinct                (20%) and δ = 0.8 (80%)—of the base vectors was randomly
baselines that represent standard, state-of-the-art, and ideal        selected, marked as deleted, and then reinserted in a single
performance scenarios:                                                batch. Fig. 5 presents the QPS-Recall@10 performance of
   • HNSW: Represents standard HNSW behavior where up-                PRO-HNSW against all baselines.
     dates involve marking nodes as deleted and reinserting              The results reveal a clear and consistent performance hier-
     nodes into vacated slots using default heuristics, without       archy. Across all datasets and conditions, PRO-HNSW consis-
     further graph optimization.                                      tently demonstrates the most favorable QPS-Recall trade-off.
   • HNSW-Rebuild(Ideal): This baseline represents the the-           Its Pareto frontiers are not only dominant over the HNSW vari-
     oretical upper bound on search quality of standard               ants and the MN-RU variants but, remarkably, also surpasses
     HNSW. After the full set of updates is performed, a new          the performance of the ideal HNSW-Rebuild baseline in all
     HNSW index is built from scratch on the final dataset.           scenarios except for the Fashion-MNIST dataset. This counter-
     While offering the best possible recall, this approach is        intuitive result, outperforming a freshly rebuilt index, strongly
     computationally prohibitive in most real-world applica-          suggests that PRO-HNSW’s three maintenance modules work
     tions.                                                           in synergy to overcome the inherent limitations of the standard
   • MN-RU γ: A state-of-the-art method from Xiao et al.              HNSW construction heuristic, producing a graph structure
     [11] that represents their core update algorithm which           that is not just repaired, but fundamentally more optimal and
     prioritizes update and query speed.                              navigable. Furthermore, another noteworthy observation is that
   • MN-THN-RU: Another state-of-the-art variant from the             PRO-HNSW consistently achieves the highest maximum recall
     same work. This method performs a more extensive                 values in almost every scenario. For instance, on the high-
     connection repair, aiming for the highest possible recall        dimensional GIST1M dataset with δ = 0.8, PRO-HNSW
     and graph stability, often at the cost of speed.                 reaches a peak recall of 84.23%, a significant 2.56 percentage
                                                                      points higher than the ideal HNSW-Rebuild baseline (81.67%)
B. Overall Performance Comparison                                     underscoring its exceptional ability to preserve the structural
  We conducted extensive experiments under two challeng-              integrity of the graph under extreme stress.
                               Fig. 6. QPS-Recall@10 comparison across datasets in consecutive update scenario.



   An interesting exception is the NYTimes dataset (Fig. 5g),           evident on the SIFT1M (Fig. 6a), DeepImage (Fig. 6c) and
where HNSW-Rebuild does not form the optimal performance                GloVe-25 (Fig. 6d) datasets, where PRO-HNSW’s Pareto
frontier. This suggests that for datasets with specific structural      frontiers are clearly dominant, significantly surpassing all
properties, such as the dense topical clusters found in docu-           baselines.
ment embeddings, a standard fresh build is not always superior.            Furthermore, PRO-HNSW consistently achieves the highest
   In contrast, MN-RU, demonstrates clear limitations. While            maximum recall across nearly all datasets and conditions. For
its variants show a slight advantage over the standard HNSW             instance, on the high-dimensional GIST1M dataset with δ =
on some datasets like Fashion-MNIST (f) and COCO-I2I (h),               0.8 (Fig. 6b), PRO-HNSW achieves a peak recall of 83.44%,
their performance on most other datasets is often comparable            which is a notable 1.77 percentage points higher than even the
or only marginally better than the degraded HNSW baseline.              ideal HNSW-Rebuild baseline (81.67%). This demonstrates its
Critically, with the exception of the NYTimes anomaly, the              remarkable ability to not just preserve but actively enhance
MN-RU variants consistently fail to surpass the ideal HNSW-             graph quality under sustained updates.
Rebuild frontier, indicating that their repair mechanisms are              In contrast, the MN-RU variants again show their limita-
less effective at achieving a truly optimal graph structure             tions, which are particularly stark on the MNIST dataset at
compared to PRO-HNSW.                                                   δ = 0.2 (Fig. 6e). In this scenario, both MN-RU variants
   Consecutive Updates. To simulate more realistic, continu-            not only fail to offer an improvement but their performance
ous data evolution, we evaluated all methods under a consec-            collapses, resulting in a Pareto frontier significantly worse than
utive update scenario. In this setup, for a given total deletion        even the standard HNSW baseline. Conversely, PRO-HNSW’s
ratio δ ∈ {0.2, 0.8}, the corresponding fraction of distinct            graph sits decisively above all other methods, including the
data vectors was randomly selected and then divided into                ideal HNSW-Rebuild, showcasing that while the MN-RU re-
1000 sequential micro-batches for iterative updates. For PRO-           pair heuristics can be detrimental on certain data distributions,
HNSW, the lightweight ROE module was applied in every                   PRO-HNSW’s comprehensive repair strategy consistently pro-
iteration, while the more intensive RDN and RUE modules                 duces a more robust and optimal graph structure in a realistic,
were invoked only once after all iterations were completed,             continuous update environment.
ensuring a balance between frequent cleaning and efficient
deep repair. Fig. 6 presents the QPS-Recall@10 performance              C. Recall Resilience under Sustained Updates
against all baselines under this sustained, iterative churn.               To evaluate the robustness and long-term performance
   The results in the consecutive update scenario reinforce the         stability of PRO-HNSW under extensive data churn, we
conclusions from the bulk updates, with PRO-HNSW consis-                conducted a demanding sustained update experiment. This
tently exhibiting the most robust and balanced performance.             scenario is distinct from the previous consecutive update
This highlights the effectiveness of PRO-HNSW’s periodic                evaluations in its intensity and the application frequency of
deep repair strategy in mitigating the cumulative damage from           our optimization modules. Here, 100% of its original data
thousands of micro-updates. This superiority is particularly            points were effectively replaced over 1000 iterations. In every
                                                                                                 TABLE II
                                                                        R ECALL @10 C OMPARISON OF I NITIAL VERSUS O PTIMIZED G RAPH

                                                                              Dataset       efs   HNSW     PRO-HNSW      ∆Recall
                                                                                            75    91.07       91.20        +0.13
                                                                             DeepImage     100    93.27       93.43        +0.16
                                                                                           125    94.71       94.86        +0.15
                                                                                            75    74.32       74.78        +0.46
                                                                              GIST1M       100    78.86       79.08        +0.22
                                                                                           125    81.95       82.13        +0.18
                                                                                            50    90.88       91.09        +0.21
                                                                              SIFT1M        75    94.34       94.50        +0.16
                                                                                           100    96.14       96.27        +0.13
                                                                                            50    70.41       70.82        +0.41
                                                                              NYTimes       75    74.65       75.02        +0.37
                                                                                           100    77.53       77.67        +0.14
                                                                                            25    95.96       96.26        +0.30
                                                                              MNIST         50    98.62       98.73        +0.11
                                                                                            75    99.23       99.26        +0.03
           Fig. 7. Recall@K comparison through iterations.
                                                                                            25    96.09       95.53        -0.56
                                                                           Fashion-MNIST    50    98.61       98.46        -0.15
                                                                                            75    99.18       99.23        +0.05
iteration, 0.1% of the total distinct data points (pre-selected                             25    88.74       88.97        +0.23
randomly from the entire dataset) were marked as deleted, and                COCO-I2I       50    94.35       94.62        +0.27
                                                                                            75    96.09       96.31        +0.22
their corresponding original data vectors were immediately
reinserted. For PRO-HNSW, after each update, all three of                                   25    71.26       71.39        +0.13
                                                                              GloVe-25      50    81.56       81.85        +0.29
its maintenance modules were sequentially applied. This sim-                                75    86.42       86.65        +0.23
ulates a highly dynamic real-world system where the index is
continuously maintained. Performance, in terms of Recall@10,
was measured periodically at a fixed efs value of 30 for both         over all competitors.
standard HNSW and PRO-HNSW.                                              This significant and sustained high performance is a direct
   Fig. 7 plots the evolution of recall as a function of these        result of the iterative application of PRO-HNSW’s three core
1000 update iterations on the Fashion-MNIST dataset. To               maintenance modules. This continuous, fine-grained mainte-
provide a more comprehensive analysis as requested by our             nance cycle not only counteracts structural decay but also
reviewers, we present the results for Recall@K across K=5,            progressively optimizes the graph structure, leading to a robust
10, 50, 100, and include a comparison against the MN-RU               and superior recall performance across all search depths (K).
variants (γ and THN-RU).
   The results illustrate a clear performance hierarchy under         D. Static Graph Optimization (No Update Scenario)
this rigorous update protocol. For all tested K values, the              Beyond addressing performance degradation in dynamic
standard HNSW and both MN-RU variants exhibit a signifi-              environments, we investigated whether our proposed main-
cant initial drop in recall, followed by a gradual, fluctuating       tenance modules could enhance the quality of an HNSW
degradation. Interestingly, while the MN-RU variants show             graph even in a static scenario, i.e., immediately after its
competitive performance at Recall@10, their effectiveness             initial construction and before any data updates occur. For
diminishes at other K values (K=5, 50, 100), where their              this “no update” experiment, we first build a standard HNSW
recall drops significantly, often performing worse than the           index for each dataset. Subsequently, we apply a subset of
standard HNSW baseline. This suggests their repair heuristics         our maintenance framework—specifically, the RDN and RUE
are specifically tuned for a certain search depth and may not         modules—to this freshly built index. The performance of this
preserve the broader neighborhood structure effectively.              post-optimized graph is labeled as PRO-HNSW in Table II.
   In stark contrast, PRO-HNSW is the only method that                The ROE module is not applied as no nodes are marked
demonstrates remarkable resilience and even performance en-           for deletion. Our primary goal here is to ascertain if these
hancement. Instead of degrading, the recall for PRO-HNSW              repair mechanisms can refine the initial graph structure formed
shows a rapid initial improvement across all K values, for            by HNSW’s construction heuristics, potentially leading to
instance rising from 97% to over 98% for Recall@10 within             improved search recall.
the first 200 iterations. It then maintains a stable, high-              Table II summarizes the Recall@10 performance. The re-
performance trajectory, ultimately stabilizing at a level sur-        sults indicate that applying our RDN and RUE modules to
passing its original state. Critically, as K increases, the perfor-   a newly built HNSW index yields a modest but consistent
mance curves for all methods become more stable, but PRO-             improvement in recall across the vast majority of datasets and
HNSW consistently maintains a significant performance gap             efs configurations. An exception was observed with Fashion-
                                                            TABLE III
                      C UMULATIVE O PERATIONAL M ETRICS AND T IME C OST B REAKDOWN OF O PTIMIZATION M ODULES .

                           ROE                         RDN                          RUE               Other      Total Update Time (s)
Dataset
                Removed Edges     Time(%)    Repaired Nodes   Time(%)    Resolved Edges    Time(%)   Time(%)     HNSW      PRO-HNSW
DeepImage            31,021,679     0.17             53,783     0.91          61,838,755    2.69       96.22     1606.73        1618.31
GIST1M                1,352,758     0.03            150,864     0.82           6,365,267    3.02       96.12      378.52         375.71
SIFT1M                2,354,398     0.20              1,697     0.31           4,318,572    2.80       96.69       67.05          77.06
NYTimes                 705,881     0.04              1,522     0.11           1,106,053    7.20       92.66       71.64          75.02
MNIST                    87,277     0.16                316     0.35             158,081    1.70       97.79        2.42           3.36
Fashion-MNIST            66,416     0.21              3,651     0.54             153,863    2.06       97.19        2.59           2.71
COCO-I2I                134,995     0.17              4,015     0.47             293,702    2.57       96.78        4.76           5.70
GloVe-25              1,957,006     0.42              4,579     0.79           2,701,798    4.18       94.60       34.08          36.11



MNIST, where a slight decrease in recall occurred at lower efs      Table III. For GIST1M dataset, PRO-HNSW (375.71 seconds)
values. This particular outcome might suggest that for some         was slightly faster than standard HNSW (378.52 seconds),
specific data distributions, the structural modifications aimed     even while performing extensive graph repairs. For most
at improving global graph quality could marginally alter some       other datasets, such as DeepImage, PRO-HNSW’s total update
optimal search paths effective only at lower search budgets.        time (1618.31s) showed only a minimal increase (less than
Nevertheless, the predominant positive trend across seven of        1%) compared to standard HNSW (1606.73s). This highly
eight datasets points towards a clear, beneficial refinement of     comparable time cost yields definitive improvements in search
the graph structure. These findings suggest that the HNSW           accuracy, with PRO-HNSW achieving a higher Recall@10
construction process may not always yield a perfectly optimal       across all datasets. This slight overhead is a modest trade-off
graph, and our lightweight modules can act as a beneficial          for the substantial gains achieved in recall.
post-construction refinement step.                                     Furthermore, Table III quantifies the substantial amount
                                                                    of structural rectification performed by PRO-HNSW. For in-
E. Efficiency of the Maintenance Modules                            stance, on SIFT1M, the ROE module removed over 2.35
   A critical aspect of our proposed framework, PRO-HNSW,           million obsolete edges, RDN repaired nearly 1,700 nodes, and
is its ability to deliver significant search performance im-        RUE resolved over 4.31 million unidirectional edges. These
provements with minimal computational overhead from its             results confirm that our modules are actively identifying and
maintenance modules. To quantify this, we analyze both the          addressing a significant volume of graph inconsistencies.
operational metrics of each module and their respective time           PRO-HNSW thus achieves a compelling balance: the main-
costs relative to the overall update process, comparing the total   tenance modules are demonstrably lightweight in terms of
update time of PRO-HNSW with that of standard HNSW. The             their time cost percentage, they perform a significant amount
results discussed in this subsection, including those presented     of corrective work on the graph structure, and the resulting
in Table III, correspond to a challenging bulk update scenario      total update time for PRO-HNSW is highly competitive. This
(Section IV-B) with a deletion ratio δ = 0.8 and an efs setting     efficiency, combined with the substantial search performance
of 75 used for evaluating the PRO-HNSW components.                  benefits, positions PRO-HNSW as a practical and effective
   Table III provides a detailed breakdown of the operational       solution for dynamic HNSW environments, offering a far more
metrics (number of edges/nodes optimized) and the percentage        economical alternative to periodic full index rebuilds.
of total PRO-HNSW update time consumed by each mod-
ule for all eight benchmark datasets under these conditions.        F. Analysis of Individual Modules
The data clearly demonstrates the lightweight nature of our            To understand the individual contribution of each proposed
individual optimization modules. For instance, in the Deep-         maintenance module, we conducted an analysis study where
Image dataset, ROE, RDN, and RUE contribute only 0.17%,             each optimization function was applied in isolation on top
0.91%, and 2.69% of the total PRO-HNSW update time,                 of the standard HNSW framework. The experiments were
respectively. This trend is consistent across other datasets.       performed under the consecutive update scenario, focusing on
The “Other” component, which encompasses the inherent               the SIFT1M and GIST1M datasets. These two were chosen as
HNSW reinsertion process, consistently accounts for the vast        they represent distinct and challenging characteristics within
majority of the update time in PRO-HNSW (e.g., 96.22% for           our benchmark suite: SIFT1M is a well-known dataset of local
DeepImage and 96.69% for SIFT1M). This underscores that             image features with moderate dimensionality, while GIST1M
the primary cost within PRO-HNSW’s update phase still stems         consists of very high-dimensional global image descriptors,
from the fundamental mark-and-replace operations, with our          allowing us to evaluate our modules under different data
optimization modules adding only a marginal overhead.               distributions and structural complexities. Fig. 8 illustrates the
   Despite these additional targeted repair steps, the total        QPS-Recall@10 performance. Each pair of subplots (e.g., (a)
update time of PRO-HNSW remains highly competitive with             SIFT1M and (b) GIST1M for ROE) shows the effect of a
that of standard HNSW, as shown in the final two columns of         single optimization module. Within each subplot, four Pareto
                                                                    HNSW+RDN initially achieves a higher maximum recall than
                                                                    standard HNSW. However, a crossover is observed where,
                                                                    beyond approximately 2.5k QPS, the recall for HNSW+RDN
                                                                    slightly underperforms standard HNSW. This might suggest
                                                                    that while RDN effectively reconnects the queried discon-
                                                                    nected nodes, the newly formed edges in the dense GIST1M
                                                                    graph might, under higher search efforts, occasionally lead to
                                                                    less globally optimal paths compared to the HNSW structure
                                                                    for this specific query set.
                                                                       Impact of RUE. The contribution of the RUE module
                                                                    is depicted in Fig. 8e and 8f. The application of RUE
                                                                    consistently leads to improved Recall@10 performance over
                                                                    standard HNSW across both SIFT1M and GIST1M datasets
                                                                    for both δ = 0.2 and δ = 0.8. The Pareto frontiers for
                                                                    HNSW+RUE are visibly superior, indicating that resolving
                                                                    unidirectional edges enhances graph navigability and allows
                                                                    the search to discover true nearest neighbors more effectively.
                                                                    This consistent improvement highlights the general benefit of
                                                                    ensuring more balanced connectivity.
                                                                       This analysis confirms that each module provides a distinct
                                                                    benefit: ROE offers foundational cleaning, RDN is critical for
                                                                    repairing reachability, and RUE consistently improves navi-
                                                                    gability. These individual contributions underscore their im-
                                                                    portance within the comprehensive PRO-HNSW framework,
                                                                    which achieves its superior performance by addressing these
                                                                    varied degradation factors synergistically.

                                                                                          V. C ONCLUSION
 Fig. 8. QPS-Recall@10 comparison of individual PRO-HNSW modules.
                                                                       In this paper, we introduced PRO-HNSW, a novel and
                                                                    lightweight maintenance framework designed to preserve and
frontiers are presented, comparing standard HNSW against            enhance the performance of HNSW graphs in dynamic envi-
HNSW augmented with the specific module, for both δ = 0.2           ronments. By systematically addressing obsolete edges, node
and δ = 0.8.                                                        disconnections, and unidirectional edges through three tar-
   Impact of ROE. The effect of applying only the ROE               geted algorithmic modules (ROE, RDN, and RUE), PRO-
module is shown in Fig. 8a and 8b For the SIFT1M dataset, the       HNSW consistently outperformed standard HNSW update
ROE module provides a discernible improvement in the QPS-           mechanisms across eight diverse datasets under various update
Recall trade-off over standard HNSW, particularly at higher         scenarios, including bulk and consecutive updates. Our exper-
recall targets for both δ = 0.2 and δ = 0.8. This suggests          iments demonstrated significant improvements in Recall@10
that pruning obsolete edges, even as a standalone measure,          and QPS with only modest computational overhead. The
enhances search efficiency. For the GIST1M dataset, however,        study of analysis of individual modules confirmed the positive
particularly under the δ = 0.8 update scenario (Fig. 8b), the       contribution of each module, and notably, we found that the
Pareto frontier of HNSW+ROE closely aligns with that of             same modules are so effective that they can improve the recall
standard HNSW, indicating a more limited impact of solely           of even a freshly built static HNSW index. This highlights
removing obsolete edges in this specific high-dimensional           that PRO-HNSW serves not just as a reactive repair tool, but
context after significant churn.                                    as a proactive graph optimization framework. PRO-HNSW
   Impact of RDN. Fig. 8c and 8d illustrate the impact of the       thus offers a practical and robust solution for enhancing
RDN module. For this particular experiment, to directly verify      the resilience and search performance of HNSW indexes in
the effectiveness of RDN in reconnecting isolated nodes, the        evolving real-world applications, making graph-based ANNS
query set was uniquely constructed: after the update process,       more viable for dynamic real-world applications.
existing disconnected nodes within the graph were identified,
and these specific nodes were then used as queries. On                                  ACKNOWLEDGMENT
SIFT1M (Fig. 8c), applying the RDN module results in a very
clear and substantial improvement in Recall@10 across the             This research was supported by the National Research
QPS spectrum for both δ = 0.2 and δ = 0.8 when compared             Foundation (NRF) funded by the Korean government (MSIT)
to standard HNSW. For GIST1M (Fig. 8d) with δ = 0.8,                (No. RS-2023-00229822).
                               R EFERENCES                                         [23] B. Harwood and T. Drummond, “Fanng: Fast approximate nearest
                                                                                        neighbour graphs,” in Proceedings of the IEEE Conference on Computer
 [1] P. Covington, J. Adams, and E. Sargin, “Deep neural networks for                   Vision and Pattern Recognition, 2016, pp. 5713–5722.
     youtube recommendations,” in Proceedings of the 10th ACM conference           [24] C. Fu, C. Xiang, C. Wang, and D. Cai, “Fast approximate nearest
     on recommender systems, 2016, pp. 191–198.                                         neighbor search with the navigating spreading-out graph,” arXiv preprint
 [2] R. Chen, B. Liu, H. Zhu, Y. Wang, Q. Li, B. Ma, Q. Hua, J. Jiang,                  arXiv:1707.00143, 2017.
     Y. Xu, H. Deng et al., “Approximate nearest neighbor search under             [25] S. Jayaram Subramanya, F. Devvrit, H. V. Simhadri, R. Krishnawamy,
     neural similarity metric for large-scale recommendation,” in Proceedings           and R. Kadekodi, “Diskann: Fast accurate billion-point nearest neighbor
     of the 31st ACM International Conference on Information & Knowledge                search on a single node,” Advances in neural information processing
     Management, 2022, pp. 3013–3022.                                                   Systems, vol. 32, 2019.
 [3] S. Okura, Y. Tagami, S. Ono, and A. Tajima, “Embedding-based news             [26] M. Aumüller, E. Bernhardsson, and A. Faithfull, “Ann-benchmarks:
     recommendation for millions of users,” in Proceedings of the 23rd ACM              A benchmarking tool for approximate nearest neighbor algorithms,”
     SIGKDD international conference on knowledge discovery and data                    Information Systems, vol. 87, p. 101374, 2020.
     mining, 2017, pp. 1933–1942.                                                  [27] M. Wang, X. Xu, Q. Yue, and Y. Wang, “A comprehensive survey and
 [4] J. Li, H. Liu, C. Gui, J. Chen, Z. Ni, N. Wang, and Y. Chen, “The                  experimental comparison of graph-based approximate nearest neighbor
     design and implementation of a real time visual search system on                   search,” arXiv preprint arXiv:2101.12631, 2021.
     jd e-commerce platform,” in Proceedings of the 19th International             [28] Y. Malkov, A. Ponomarenko, A. Logvinov, and V. Krylov, “Approximate
     Middleware Conference Industry, 2018, pp. 9–16.                                    nearest neighbor algorithm based on navigable small world graphs,”
 [5] J. Johnson, M. Douze, and H. Jégou, “Billion-scale similarity search              Information Systems, vol. 45, pp. 61–68, 2014.
     with gpus,” IEEE Transactions on Big Data, vol. 7, no. 3, pp. 535–547,        [29] M. Iwasaki, “Neighborhood graph and tree for indexing highdimensional
     2019.                                                                              data,” Yahoo Japan Corporation. Retrieved August, vol. 22, p. 2020,
                                                                                        2015.
 [6] P. Lewis, E. Perez, A. Piktus, F. Petroni, V. Karpukhin, N. Goyal,
                                                                                   [30] Q. Chen, H. Wang, M. Li, G. Ren, S. Li, J. Zhu, J. Li, C. Liu, L. Zhang,
     H. Küttler, M. Lewis, W.-t. Yih, T. Rocktäschel et al., “Retrieval-
                                                                                        and J. Wang, “Sptag: A library for fast approximate nearest neighbor
     augmented generation for knowledge-intensive nlp tasks,” Advances in
                                                                                        search,” 2018.
     neural information processing systems, vol. 33, pp. 9459–9474, 2020.
                                                                                   [31] Z. Jin, D. Zhang, Y. Hu, S. Lin, D. Cai, and X. He, “Fast and accurate
 [7] H. Li, Y. Su, D. Cai, Y. Wang, and L. Liu, “A survey on retrieval-
                                                                                        hashing via iterative nearest neighbors expansion,” IEEE transactions
     augmented text generation,” arXiv preprint arXiv:2202.01110, 2022.
                                                                                        on cybernetics, vol. 44, no. 11, pp. 2167–2177, 2014.
 [8] K. Santhanam, O. Khattab, J. Saad-Falcon, C. Potts, and M. Zaharia,           [32] C. Fu and D. Cai, “Efanna: An extremely fast approximate near-
     “Colbertv2: Effective and efficient retrieval via lightweight late interac-        est neighbor search algorithm based on knn graph,” arXiv preprint
     tion,” arXiv preprint arXiv:2112.01488, 2021.                                      arXiv:1609.07228, 2016.
 [9] Y. A. Malkov and D. A. Yashunin, “Efficient and robust approxi-               [33] W. Li, Y. Zhang, Y. Sun, W. Wang, M. Li, W. Zhang, and
     mate nearest neighbor search using hierarchical navigable small world              X. Lin, “Approximate nearest neighbor search on high dimensional
     graphs,” IEEE transactions on pattern analysis and machine intelligence,           data—experiments, analyses, and improvement,” IEEE Transactions on
     vol. 42, no. 4, pp. 824–836, 2018.                                                 Knowledge and Data Engineering, vol. 32, no. 8, pp. 1475–1488, 2019.
[10] A. Singh, S. J. Subramanya, R. Krishnaswamy, and H. V. Simhadri,              [34] J. V. Munoz, M. A. Gonçalves, Z. Dias, and R. d. S. Torres, “Hierarchical
     “Freshdiskann: A fast and accurate graph-based ann index for streaming             clustering-based graphs for large scale approximate nearest neighbor
     similarity search,” arXiv preprint arXiv:2105.09613, 2021.                         search,” Pattern Recognition, vol. 96, p. 106970, 2019.
[11] W. Xiao, Y. Zhan, R. Xi, M. Hou, and J. Liao, “Enhancing hnsw index           [35] C. Fu, C. Wang, and D. Cai, “High dimensional similarity search
     for real-time updates: Addressing unreachable points and performance               with satellite system graph: Efficiency, scalability, and unindexed query
     degradation,” arXiv preprint arXiv:2407.07871, 2024.                               compatibility,” IEEE Transactions on Pattern Analysis and Machine
[12] R. Bellman, “Dynamic programming,” science, vol. 153, no. 3731, pp.                Intelligence, vol. 44, no. 8, pp. 4139–4150, 2021.
     34–37, 1966.                                                                  [36] S. Yu, S. Lin, S. Gong, Y. Xie, R. Liu, Y. Zhou, J. Sun, Y. Zhang, G. Li,
[13] H. Jegou, M. Douze, and C. Schmid, “Product quantization for nearest               and G. Yu, “A topology-aware localized update strategy for graph-based
     neighbor search,” IEEE transactions on pattern analysis and machine                ann index,” arXiv preprint arXiv:2503.00402, 2025.
     intelligence, vol. 33, no. 1, pp. 117–128, 2010.                              [37] F. Zardbani, N. Mamoulis, S. Idreos, and P. Karras, “Adaptive indexing
[14] T. Cover and P. Hart, “Nearest neighbor pattern classification,” IEEE              of objects with spatial extent,” Proceedings of the VLDB Endowment,
     transactions on information theory, vol. 13, no. 1, pp. 21–27, 1967.               vol. 16, no. 9, pp. 2248–2260, 2023.
[15] A. Guttman, “R-trees: A dynamic index structure for spatial searching,”       [38] A. Babenko and V. Lempitsky, “Efficient indexing of billion-scale
     in Proceedings of the 1984 ACM SIGMOD international conference on                  datasets of deep descriptors,” in Proceedings of the IEEE Conference
     Management of data, 1984, pp. 47–57.                                               on Computer Vision and Pattern Recognition, 2016, pp. 2055–2063.
[16] J. L. Bentley, “Multidimensional binary search trees used for associative     [39] D. Newman, “Bag of Words,” UCI Machine Learning Repository, 2008,
     searching,” Communications of the ACM, vol. 18, no. 9, pp. 509–517,                DOI: https://doi.org/10.24432/C5ZG6P.
     1975.                                                                         [40] Y. LeCun, L. Bottou, Y. Bengio, and P. Haffner, “Gradient-based learning
[17] S. Dasgupta and Y. Freund, “Random projection trees and low dimen-                 applied to document recognition,” Proceedings of the IEEE, vol. 86,
     sional manifolds,” in Proceedings of the fortieth annual ACM symposium             no. 11, pp. 2278–2324, 2002.
     on Theory of computing, 2008, pp. 537–546.                                    [41] H. Xiao, K. Rasul, and R. Vollgraf, “Fashion-mnist: a novel image
[18] S. Dasgupta and K. Sinha, “Randomized partition trees for exact nearest            dataset for benchmarking machine learning algorithms,” arXiv preprint
     neighbor search,” in Conference on learning theory. PMLR, 2013, pp.                arXiv:1708.07747, 2017.
     317–337.                                                                      [42] T.-Y. Lin, M. Maire, S. Belongie, J. Hays, P. Perona, D. Ramanan,
[19] P. Indyk and R. Motwani, “Approximate nearest neighbors: towards                   P. Dollár, and C. L. Zitnick, “Microsoft coco: Common objects in
     removing the curse of dimensionality,” in Proceedings of the thirtieth             context,” in Computer vision–ECCV 2014: 13th European conference,
     annual ACM symposium on Theory of computing, 1998, pp. 604–613.                    zurich, Switzerland, September 6-12, 2014, proceedings, part v 13.
[20] Q. Chen, B. Zhao, H. Wang, M. Li, C. Liu, Z. Li, M. Yang, and J. Wang,             Springer, 2014, pp. 740–755.
     “Spann: Highly-efficient billion-scale approximate nearest neighborhood       [43] J. Pennington, R. Socher, and C. D. Manning, “Glove: Global vectors
     search,” Advances in Neural Information Processing Systems, vol. 34,               for word representation,” in Proceedings of the 2014 conference on
     pp. 5199–5212, 2021.                                                               empirical methods in natural language processing (EMNLP), 2014, pp.
[21] A. Andoni and I. Razenshteyn, “Optimal data-dependent hashing for                  1532–1543.
     approximate near neighbors,” in Proceedings of the forty-seventh annual
     ACM symposium on Theory of computing, 2015, pp. 793–801.
[22] M. Datar, N. Immorlica, P. Indyk, and V. S. Mirrokni, “Locality-sensitive
     hashing scheme based on p-stable distributions,” in Proceedings of the
     twentieth annual symposium on Computational geometry, 2004, pp. 253–
     262.
