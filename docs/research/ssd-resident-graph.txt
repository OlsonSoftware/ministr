                                          Optimizing SSD-Resident Graph Indexing for High-Throughput
                                                                Vector Search
                                                          Weichen Zhao∗                                              Yuncheng Lu∗                                     Yao Tian
                                                  East China Normal University                                      Bytedance                                        Bytedance
                                                  weichenzhao@stu.ecnu.edu.cn                               luyuncheng@bytedance.com                         yao.tian@bytedance.com

                                                              Hao Zhang                                                  Jiehui Li                               Minghao Zhao†
                                                           Bytedance                                                    Bytedance                         East China Normal University
                                                   zhanghao.ai@bytedance.com                                    lijiehui@bytedance.com                     mhzhao@dase.ecnu.edu.cn




arXiv:2602.22805v1 [cs.DB] 26 Feb 2026
                                                                                           Yakun Li†                                       Weining Qian
                                                                                         Bytedance                                   East China Normal University
                                                                                liyakun.hit@bytedance.com                             wnqian@dase.ecnu.edu.cn

                                         ABSTRACT                                                                               1     INTRODUCTION
                                         Graph-based approximate nearest neighbor search (ANNS) meth-                           Approximate nearest neighbor search (ANNS) serves as the core
                                         ods (e.g., HNSW) have become the de facto state of the art for their                   technique behind applications like recommendation [31, 32, 46] and
                                         high precision and low latency. To scale beyond main memory,                           retrieval-augmented generation (RAG) [30]. Among various solu-
                                         recent out-of-memory ANNS systems leverage SSDs to store large                         tions to the ANN problem, graph-based methods organize vectors
                                         vector indexes. However, they still suffer from severe CPU underuti-                   into proximity graphs and retrieve neighbors via greedy traversal;
                                         lization and read amplification (i.e., storage stalls) caused by limited               these approach have emerged as the de facto state-of-the-art due to
                                         access locality during graph traversal. We present VeloANN, which                      their superior accuracy-efficiency balance. Nevertheless, existing
                                         mitigates storage stalls through a locality-aware data layout and                      designs typically assume an in-memory execution model to achieve
                                         a coroutine-based asynchronous runtime. VeloANN utilizes hierar-                       low latency, which becomes impractical when modern embedding
                                         chical compression and affinity-based data placement scheme to                         collections grow to billions of vectors. Therefore, recent efforts turn
                                         co-locate related vectors within the same page, effectively reduc-                     to out-of-memory ANN systems that seek to scale capacity beyond
                                         ing fragmentation and over-fetching. We further design a record-                       DRAM while retaining near in-memory performance [10, 31, 46].
                                         level buffer pool, where each record groups the neighbors of a                             To enable out-of-memory (a.k.a larger-than-memory) vector
                                         vector; by persistently retaining hot records in memory, it elimi-                     search, systems such as DiskANN [23] and its successors [37, 41,
                                         nates excessive page swapping under constrained memory budgets.                        44, 50] adopt a page-based storage layout, where each page holds
                                         To minimize CPU scheduling overheads during disk I/O interrup-                         multiple fixed-size records, and each record contains a vector and
                                         tions, VeloANN employs a coroutine-based asynchronous runtime                          its neighbor identifiers. The search process follows the same best-
                                         for lightweight task scheduling. On top of this, it incorporates                       first paradigm as in in-memory graphs, starting from a chosen
                                         asynchronous prefetching and a beam-aware search strategy to pri-                      (or random) entry point and iteratively fetching neighbors from
                                         oritize cached data, ultimately improving overall search efficiency.                   disk until convergence. To manage memory–disk interaction, pro-
                                         Extensive experiments show that VeloANN outperforms state-of-                          duction systems adopt two designs. (1) OS-managed caching via
                                         the-art disk-based ANN systems by up to 5.8× in throughput and                         memory-mapped I/O (e.g., mmap [5, 42]), used by Qdrant [40] and
                                         3.25× in latency reduction, while achieving 0.92× the throughput                       OpenSearch [37], offers simplicity but limited control. (2) Self-
                                         of in-memory systems using only 10% of their memory footprint.                         managed buffer pools with custom replacement policies, such as in
                                                                                                                                PgVector [1], provide explicit control over hot-page residency at
                                         PVLDB Reference Format:                                                                the cost of higher complexity (§2.4).
                                         Weichen Zhao, Yuncheng Lu, Yao Tian, Hao Zhang, Jiehui Li, Minghao                         In fact, however, current systems simply reuse generic OS or DBMS
                                         Zhao, Yakun Li, and Weining Qian. Optimizing SSD-Resident Graph                        mechanisms, overlooking the access characteristics of graph-based
                                         Indexing for High-Throughput Vector Search. PVLDB, 14(1): XXX-XXX,
                                                                                                                                ANN search, and thus fail to deliver satisfactory performance. In
                                         2026.
                                                                                                                                such cases, graph traversal exhibits poor spatial locality, causing
                                         doi:XX.XX/XXX.XX
                                                                                                                                excessive random I/O and significant CPU underutilization. As a
                                         ∗ Weichen Zhao and Yuncheng Lu contributed equally to this work.                       result, state-of-the-art methods achieve less than half of the query
                                         † Corresponding authors.
                                                                                                                                throughput (QPS) of their in-memory counterparts (Fig. 1). As the
                                         This work is licensed under the Creative Commons BY-NC-ND 4.0 International            result of the storage stalls, performance gap primarily stems from
                                         License. Visit https://creativecommons.org/licenses/by-nc-nd/4.0/ to view a copy of
                                         this license. For any use beyond those covered by this license, obtain permission by   the following factors.
                                         emailing info@vldb.org. Copyright is held by the owner/author(s). Publication rights
                                         licensed to the VLDB Endowment.
                                                                                                                                    • The serialized execution of I/O and computation leads to
                                         Proceedings of the VLDB Endowment, Vol. 14, No. 1 ISSN 2150-8097.                            both CPU and I/O underutilized. the best-first search algo-
                                         doi:XX.XX/XXX.XX                                                                             rithm [15] executes computation and I/O sequentially at each
                                                                                                                                                To optimize on-disk layouts, VeloANN first quantizes each record



    7 K U R X J K S X W   4 3 6 
                                           
                                                                                                                                                to reduce its storage footprint. The original 32-bit floating-point
                                                                                                                                                vectors are quantized into 4-bit-per-dimension codes using ExtRa-
                                           
                                                                                                                                                bitQ [12, 13], while the neighbor list is further compressed with
                                                                                                                                             efficient integer coding techniques such as delta encoding or Par-
                                                                                                                                             titioned Elias-Fano coding [38]. To support variable-sized record
                                                                                                                                               management and fast lookup, VeloANN introduces a new page lay-
                                                  ' L V N $ 1 1    6 W D U O L Q J    3 L S H $ 1 1    9 H O R $ 1 1    , Q  0 H P R U \       out, where the page head stores metadata such as record offsets
Figure 1: Query throughput comparison on the GIST1M                                                                                             and IDs, and the variable-sized records are appended from the op-
dataset among an in-memory index (Vamana [23]), on-disk                                                                                         posite end. This two-way growth design achieves dense packing to
indexes (DiskANN [23], Starling [44], PipeANN [15]), and our                                                                                    fully utilize available page space. To improve data locality, VeloANN
VeloANN. The memory budget is 20% of the disk index size.                                                                                       embeds a near-zero-overhead record coloring technique into the
                                                                                                                                                graph construction process. Upon updating the out-neighbors of
          search step, causing excessive CPU idle time while waiting for                                                                        each vertex in the initial random graph, VeloANN assigns closely
          disk reads, which are typically an order of magnitude slower                                                                          related vertex pairs a shared color and places them within the same
          than computation. In addition, its synchronous I/O execution                                                                          physical page at the end of construction. By reusing computations
          requires each step to wait for the slowest read in the batch                                                                          already performed during graph building, this design avoids costly
          to complete, leaving the I/O pipeline severely underutilized.                                                                         graph reordering while achieving the same I/O reduction effect
          PipeANN [15] overlaps the next search iteration with under-                                                                           during query traversal.
          utilized I/O periods, which reducing query latency but causes                                                                             To address the low cache efficiency issue, VeloANN redesigns
          a throughput drop due to I/O over-fetching.                                                                                           its buffer management from both the data structure and search
 • Suboptimal on-disk layouts leads to substantial over-                                                                                        perspectives. At the data structure level, it introduces a record-level
   prefetching. Current on-disk layouts typically pack multiple                                                                                 buffer pool motivated by the highly skewed access patterns in ANN
   records per page sequentially by record ID. This design suffers                                                                              workloads. For example, on the SIFT1M workload, we found that
   from poor data locality, since semantically or spatially related                                                                             47% of vertices in DiskANN’s graph remain unaccessed during
   vertices are scattered across different pages. As a result, an                                                                               queries, while only 0.1% of pages are untouched, revealing a clear
   I/O operation often loads an entire page but uses only a single                                                                              mismatch between record-level access and page-level caching. To
   record, leading to severe I/O amplification. It also suffers from                                                                            bridge this gap, VeloANN introduces a record-level buffer pool and
   page fragmentation, as fixed-size records often leave pages par-                                                                             employs a lightweight clock-based second-chance policy to pin
   tially filled, wasting storage space and I/O bandwidth. Recent                                                                               frequently accessed records in memory and gradually evict cold
   disk-based methods [41, 44] adopt graph reordering techniques                                                                                records. At the search level, VeloANN proactively fetches promis-
   to place adjacent vertices within the same page but become                                                                                   ing on-disk candidates ahead of use and develops a cache-aware
   impractical at scale due to their high computational overhead.                                                                               beam search strategy to prioritize exploring in-memory candidates,
                                                                                                                                                achieving fewer I/O stalls and thereby better query performance.
 • Suboptimal buffer management yields low cache hit rates.                                                                                     In summary, we make the following contributions:
   Existing paging mechanisms fail to capture the access charac-
                                                                                                                                                • We design an asynchronous coroutine-based execution frame-
   teristics of ANN workloads, resulting in low cache hit rate and,
                                                                                                                                                  work that dynamically schedules ready queries while others are
   consequently, degraded query performance. For instance, on
                                                                                                                                                  suspended on I/O, maximizing CPU and SSD utilization.
   the SIFT1M dataset, when the buffer cache is set to 8% of the in-
                                                                                                                                                • We propose a novel on-disk page layout that clusters adjacent
   dex size, the cache hit rate reaches only 8.49% for DiskANN [23].
                                                                                                                                                  compressed records into the same page to reduce redundant I/O.
   Even with a generous 20% buffer allocation, the cache hit rate
                                                                                                                                                • We develop a record-level buffer management policy, comple-
   increases only to 24.4%, indicating ineffective caching design.
                                                                                                                                                  mented by asynchronous prefetching and cache-aware beam
   To tackle these challenges, we design VeloANN, an out-of-memory                                                                                search to better exploit access skew and improve cache hit rates.
graph-based ANNS system that eliminates storage stalls through a                                                                                • We implement VeloANN1 in Rust with production-grade relia-
holistic co-design of compute–I/O scheduling, on-disk data layout,                                                                                bility. Extensive experiments demonstrate that VeloANN deliv-
and buffer management. As shown in Figure 1, VeloANN delivers                                                                                     ers up to 5.8× higher QPS and 3.25× lower query latency than
near in-memory performance. To achieve this, VeloANN adopts                                                                                       state-of-the-art disk-based ANNS systems, while approaching
a coroutine-based asynchronous execution model to fully utilize                                                                                   the performance of leading in-memory indexes, e.g., using only
both CPU and I/O resources. Each query is executed as a corou-                                                                                    10% of their memory, it achieves 0.92× QPS.
tine that interleaves distance computation and SSD I/O through
                                                                                                                                                  The rest of the paper is organized as follows. Section 2 intro-
non-blocking operations, allowing multiple query coroutines to
                                                                                                                                                duces basic concepts and background. Our VeloANN is detailed in
progress cooperatively. When a coroutine is suspending on an I/O
                                                                                                                                                Section 3 and cache-aware ANNS procedure Section 4 followed by
request, the scheduler yields control to other coroutines for compu-
                                                                                                                                                experimental results in Section 5. Section 6 reviews related work,
tation and resumes it once the I/O completes. This design effectively
                                                                                                                                                and we conclude in Section 7.
overlaps I/O latency with computation, enabling continuous hard-
ware utilization without costly thread-context switches or locking                                                                              1 The artifacts will be open-sourced after Bytedance’s review process, which is currently
overhead.                                                                                                                                       underway.
                                                                                                                                            2
2     BACKGROUND                                                                Asynchronous I/O. To overcome CPU underutilization in syn-
This section first introduces the fundamentals of ANNS and graph-               chronous I/O, where threads block awaiting I/O completion, modern
based indexes for ANNS, then reviews techniques for asynchronous                data systems employ asynchronous I/O (AIO). AIO enables threads
execution and database buffer pool management.                                  to initiate I/O operations and continue execution, receiving notifica-
                                                                                tions upon completion. This non-blocking approach is fundamental
2.1    Approximate Nearest Neighbor Search                                      for building high-performance, I/O-intensive systems. Key imple-
                                                                                mentations include the established POSIX AIO and the more recent
In this paper, we study the approximate nearest neighbor search
                                                                                Linux io_uring interface. POSIX AIO allows applications to initiate
(ANNS) problem in Euclidean space. Let the dataset be 𝑋 = {𝑥 1, . . . ,
                                                                                one or more I/O operations that are performed in the background.
𝑥𝑛 }, where each data point 𝑥𝑖 ∈ R𝑑 is a 𝑑-dimensional vector. Given
                                                                                io_uring works by creating two circular buffers, i.e., submission
a query vector 𝑞 ∈ R𝑑 , the goal of nearest neighbor search is to find
                                                                                and completion queues, to track the submission and completion of
the top-𝑘 data points in 𝑋 that are closest to 𝑞 under the Euclidean
                                                                                I/O requests, respectively. Keeping these buffers shared between
distance metric ∥·, ·∥ 2 , namely:
                                                                                the kernel and application helps boost the I/O performance by elim-
                      𝑅 = 𝑘- arg min ∥𝑞 − 𝑥𝑖 ∥ 2 .                   (1)        inating the need to issue extra and expensive system calls to copy
                                  𝑥𝑖 ∈𝑋
                                                                                these buffers between the two. It also enables batched I/O requests
Due to the curse of dimensionality [22], computing the exact top-𝑘
                                                                                to allow multiple I/O operations to be processed concurrently.
neighbors is prohibitively expensive in high dimensions. Therefore,
                                                                                Coroutines. Coroutines are special functions that can suspend
ANN search is commonly adopted, which trades a bit accuracy for
                                                                                (yield CPU time) voluntarily and be resumed later by a user-space
substantial efficiency gains. For an ANN result 𝑅 ′ with |𝑅 ′ | = 𝑘, the
                                                                                scheduler as needed. Specifically, upon issuing an asynchronous
retrieval accuracy is typically measured by Recall@k, defined as
                                                                                I/O operation, a coroutine can suspend execution, allowing the
                                     |𝑅 ∩ 𝑅 ′ |                                 scheduler to run another ready coroutine; once the I/O completes,
                        Recall@k =              .                     (2)
                                        𝑘                                       it resumes to continue its task. Stackless coroutines, as standard-
                                                                                ized in C++20 and Rust, provide a concurrency model with minimal
                                                                                overhead for construction and context switching, often less than
2.2    Graph-based ANNS Index                                                   the cost of a last-level cache miss [19]. Unlike traditional coroutines,
As reviewed in Section 6, numerous graph-based indexes have been                they do not maintain their own stacks; instead, they execute on the
proposed for ANN queries [3, 4, 8–10, 18, 34, 39, 45]. These ap-                call stack of the runtime scheduler. While coroutine invocation re-
proaches achieve state-of-the-art search performance by efficiently             sembles a standard function call, its state—including local variables
identifying nearest neighbors through graph traversal, typically                that persist across suspension points—is preserved in dynamically
examining only a limited number of candidate vectors [35]. Specifi-             allocated memory that survives suspend and resume cycles.
cally, during the indexing phase, these methods construct a graph                   For instance, Rust natively supports asynchronous programming.
structure where each data vector corresponds to a vertex. Upon                  It abstracts asynchronous operations with the Future trait, which
receiving a query, the system employs a greedy beam search al-                  represents a computation whose result may not yet be available
gorithm [9, 10, 23, 34]. Throughout the search process, the algo-               (e.g., I/O operations) and makes progress when polled. With Rust’s
rithm maintains a beam set with a maximum capacity of 𝑛𝑏 vectors.               syntactic sugar (e.g., await), asynchronous I/O can be expressed
The search initiates by adding an entry point to the beam set and               in a sequential style, while a pluggable runtime scheduler (i.e., the
proceeds iteratively. In each iteration, the algorithm identifies the           scheduling logic can be customized) schedules and executes other
unvisited vector within the beam set that is closest to the query               tasks during I/O wait periods.
vector, marks it as visited, and computes exact distances between
the query vector and all its graph neighbors. These neighbors are
subsequently inserted into the beam set. If the beam set exceeds its            2.4    Larger-than-Memory Database Techniques
capacity 𝑛𝑏 after insertion, only the 𝑛𝑏 nearest vectors are retained.          While in-memory databases offer superior performance, their ca-
The algorithm terminates when all vectors in the beam set have                  pacity is limited by the available main memory. To handle datasets
been marked as visited, at which point the nearest vector in the                that exceed this capacity, database systems must utilize secondary
beam set is returned as the result. It is evident that a larger beam            storage and manage the movement of data pages between memory
size results in the examination of more vectors during search, con-             and disk. The techniques for managing this data swapping can be
sequently yielding higher accuracy at the cost of increased latency.            broadly categorized into two types:
                                                                                OS-controlled caching. Memory-mapped (mmap) file I/O is an
2.3    Asynchronous Execution Framework                                         OS-provided feature that maps file contents on secondary storage
Modern I/O-intensive data systems increasingly adopt asynchro-                  into a process’s virtual address space, allowing the database to
nous execution models to optimize performance and resource uti-                 access pages via pointers as if they resided entirely in memory. The
lization. This model leverages asynchronous I/O to allow issuing                OS transparently loads pages on-demand upon access and evicts
multple I/O requests in the background, so that the thread don’t                them according to its internal policies when memory pressure
need to block on the synchronous I/O. Complementary with this,                  arises. Owing to its simplicity and low engineering cost, many
organizing and scheduling tasks as coroutines—functions that can                vector databases (e.g., Qdrant [40], OpenSearch [37]) employ mmap
suspend and resume its execution—to effectively overlap computa-                to handle larger-than-memory datasets. However, as Crotty et al. [5]
tion with I/O operations.                                                       argue, mmap is often ill-suited for DBMSs because the DBMS loses
                                                                            3
                                        async prefetch(pids):                       1. async fn search(q) {                                 Pefetch and cache hit            Control flow
           Thread-per-Core               …                                              … //Init the candidate
                                      async FetchPage(pid):
                                         submit_io(pid).await                       2. while let Some(vid)=candidate.pop(){
      Worker thread 1                  … wait_for_completion
                                       submit_io(pid).await
                                         parse_page
                                                                                    3.    spawn(prefetch(candidate.top(N)))
                                                                                                                                           single                                single
                                                                                    4.    record = bp.get(vid).await
                                       wait_for_completion                                                                                 thread                                thread
                                         …                                          5.    full_ret.push(record)
                             ❹ switch parse_page                                                                                                                  query 1           query 2
       Scheduler                       …                        ❸ async I/O
                                                                                    6.
                                                                                    7.
                                                                                          dists = compute_dists(record.ad)
                                                                                          candidate.push(dists)
                                                                                    8. }                                                                          Vertex 1
    ❶ execute
                                                                                                                                query 1
                                                                                    9. return full_ret.sort();                            Compute                                    Vertex 3
                                                                                    10.}                                                                          Compute
             Query 1                                                 Disk                                                                 Vertex 2
                                                                                    (a) ANNS Async Search Procedure
             (coroutine)                                                                                                                  Compute                 Vertex 2
           read(v0).await    ❷ prefetch & read v0                                   1. fn schedule(ctx, future) {
                                                                                                                                            …
                                                                                    2. while poll(future) {                                                       Compute
            dist(q1, v0)
                                    VID     Pos                   Rec 0             3.   //Consume all tasks
                 …                                                                  4.   while let Some(t)=ctx.tasks.pop(){               Vertex 3
                                                                                                                                                                  Vertex 2           Compute

                                      0      P0                     …
                                                                                    5.    t.run(); //resume the coroutine                 Compute

             Query 2                                                                                                          query 2
                                                                                          //Submit I/Os and Process the CQ                                        Compute            Vertex 3
                                      1                                             6.     ctx.io_uring.submit()
                                                                                                                                          Vertex 4
❺ switch     (coroutine)                                                            7.    }                                                                                          Compute

           read(v1).await            …       …                    Rec 1             8.    //Wait and Process CQ                           compute
                                                                                    9.    ctx.io_uring.park()                                                        …                    …
                 …                                                                  10. }
                                                                                                                                            …
                                   Record Mapping                Buffer Pool
                …                                                                   11.}
                                                                                    (b) Scheduler Logic                            (c) Sync Execution       vs.     Async Execution


Figure 2: Overview of asynchronous execution in VeloANN.
                                                                                   Figure 3: Coroutine-based ANNS Execution. Each thread has
                                                                                   its own scheduler that batches incoming queries and inter-
                                                                                   leaves multiple query coroutines.
control over page faulting and eviction, and the OS’s virtual memory
subsystem can be too slow for modern NVMe SSDs.
Self-Managed Caching. To gain full control over page loading                       Through these I/O-hiding mechanisms, VeloANN sustains high CPU
and eviction, many databases implement their own buffer man-                       utilization by reducing I/O stalls along the query execution path.
agement in user space. The traditional approach uses a hash table                     This section details the design of VeloANN. We first introduce its
to map page identifiers (PIDs) to their location in the buffer pool.               coroutine-based asynchronous execution model, which overlaps
Accessing a page thus requires a hash table lookup, and a miss                     computation and I/O (§3.1). We then describe the on-disk page
triggers a read from secondary storage. While flexible, the overhead               layout (§3.3) and record-level buffer pool that enable the index to
of these lookups can be substantial. To mitigate this cost, recent                 scale beyond main memory capacity (§3.2).
designs employ more memory-efficient techniques. For instance,
LeanStore [27] uses pointer swizzling [14, 26], which replaces a                   3.1     Coroutine-based ANNS Execution
page’s PID with its direct memory pointer within other data struc-                 Existing disk-based ANNS systems like DiskANN [23] suffer from
tures. This allows page accesses on a cache hit to be performed via a              significant CPU underutilization. Our measurements show only 57%
direct pointer dereference, bypassing the hash table. However, this                CPU utilization on Sift1M, as their sequential compute-I/O model
technique requires strict exclusive ownership control (i.e., a page                forces the CPU to idle during disk reads, which are an order of mag-
can only be referenced by a single pointer) to handle concurreny                   nitude slower than computation. Although beam search mitigates
control, making it unsuitable for graph structures where a node                    this by reading multiple records concurrently, the synchronous
can have multiple references. Another approach, VMCache [28],                      execution pattern persists. Each search step remains bottlenecked
builds on mmap but reclaims control by using the madvise system                    by the slowest I/O operation, preventing high CPU utilization. To
call to provide caching and eviction hints to the OS, combining the                address this bottleneck, VeloANN introduces a coroutine-based exe-
simplicity of mmap with the control of a self-managed cache.                       cution model with holistic redesign of the ANNS query processing
                                                                                   to hide I/O stalls. By modeling each query as a coroutine, the sched-
3   VELOANN DESIGN                                                                 uler can switch from one awaiting I/O completion to another that
We present VeloANN, a I/O-efficient ANNS system for handling                       is ready for computation. As illustrated in Fig.3, this asynchronous
workloads that exceed main-memory capacity. As shown in Fig. 2,                    query processing approach allows VeloANN to saturate the CPU:
VeloANN adopts a thread-per-core asynchronous execution model                      Asynchronous ANNS Procedure. Panel (a) in Fig.3 illustrates
where each worker thread runs a dedicated scheduler that mod-                      the asynchronous, coroutine-based ANNS procedure. The search
els incoming batched queries as coroutines. This design enables a                  begins by initializing a candidate list with entry points in the graph
worker to interleave multiple queries, hiding I/O latency by switch-               index. The asynchronous search procedure then iteratively refines
ing to a ready coroutine when another awaits data from disk (❶). A                 the list using a best-first search strategy with three main phases
running coroutine traverses the graph, issuing access requests for                 (lines 2-10): (i) The coroutine issues non-blocking prefetch requests
records it needs (❷). If a requested record is on disk (i.e., a cache              for the top-𝑁 candidate vertices to improve the cache hit rate for
miss), the coroutine initiates an asynchronous I/O operation to fetch              subsequent search steps (line 3, detailed in §4). (ii) It then requests
it (❸) and yields control (❹). If the record is already in memory, the             the vertex record (i.e., vector and adjacency list) from the buffer
coroutine continues its execution. The scheduler then switch the                   pool (line 4). If the record is not cached, the coroutine suspends
control to next ready ANNS coroutine. The scheduler completes                      and yields control to the scheduler, awaiting I/O completion (line
the cycle by polling the I/O completion queue and readying the                     5). (iii) Once the record data is loaded, the scheduler resumes the
corresponding coroutine for resumption once its data arrives (❺).                  coroutine, which then computes the distance between the query
                                                                               4
and the fetched vertex and estimates distances to its neighbors                 Table 1: Cache hit rate (%) of different page replacement
using their quantized vectors to update the candidate list (lines 5-7).         policies with varying buffer ratios on a custom page-level
This process repeats until the candidate list is exhausted.                     buffer pool for DiskANN.
I/O-Aware scheduler. As illustrated in Panel (b) of Fig.3, each
worker threads is pinned to a CPU core and runs a dedicated sched-                                                                                        10%                    20%          30%                 40%                50%
uling loop to process incoming ANNS queries in batches. The sched-
                                                                                                                                      LRU               13.0%                   25.8%       35.8%                45.1%               54.0%
uler runs a continuous loop util the main function has terminated
                                                                                                                                      FIFO              15.0%                   26.0%       35.9%                45.3%               54.3%
(line 2). When the control yields to the scheduler, the scheduler
                                                                                                                                      Random            14.9%                   25.0%       34.8%                44.1%               52.8%
then retrieves ready coroutines from the task queue and resumes
their execution (lines 4-5). When an ANNS coroutine suspends (e.g.,
on a cache miss), the scheduler submits the pending I/O requests                                                                                                                1 R G H               3 D J H
via an asynchronous I/O driver (e.g., io_uring) and processes the



                                                                                3 H U F H Q W D J H  R I  , W H P V
                                                                                                                                           X Q D F F H V V H G                                        X Q D F F H V V H G
completion queue from the interface, pushing coroutines with com-
                                                                                                                           
pleted I/O back to the task queue (line 6). If the ready queue is
                                                                                                                                             X Q D F F H V V H G                                          X Q D F F H V V H G
empty, this means all coroutines are awaiting I/O completion. In
this scenario, the scheduler busy-polls the I/O completion queue                                                                                                D   * L V W  0                                                  E   6 L I W  0 
                                                                                                                           
until a coroutine can be made ready.                                                                                                                                                                                                          
    The scheduler limits the batch size 𝐵, i.e., the number of con-                                                                                                   1 X P E H U  R I  $ F F H V V H V
currently executing queries per thread, to balance the trade-off
between throughput and latency. While a larger batch size can
improve throughput by enabling the scheduler to more effectively                Figure 4: Access frequency distributions at node- and page-
overlap I/O with computation, it may also increase average query                level granularities for DiskANN on Sift1M and Gist1M.
latency due to context switching and scheduling overhead. To op-
timize this balance, we formulate the batch size as 𝐵 = ⌈𝛼 · 𝐼 /𝑇 ⌉.
In this formula, 𝐼 is the average disk I/O latency, 𝑇 is the average           buffer pool for DiskANN, evaluating several common cache replace-
execution time of the computational task between two I/O events,               ment policies while varying the buffer size as a fraction of the index
and 𝛼 is a hyperparameter representing the expected frequency                  size. As shown in Table 1, the cache hit rate improves only linearly
of I/O events per computational task. This formulation is derived              with increased buffer allocation. This low access locality stands in
from the principle of overlapping computation with I/O to keep                 stark contrast to traditional database workloads like TPC-C, where
the CPU utilized. Since a coroutine computes for an average of 𝑇 /𝛼            80% of accesses typically target just 20% of the data [29]. Interest-
before an I/O of latency 𝐼 , the number of coroutines needed to keep           ingly, sophisticated replacement policies (e.g., LRU, FIFO) offer only
the CPU busy during that I/O is 𝐼 /(𝑇 /𝛼) = 𝛼 · 𝐼 /𝑇 . The value of 𝛼          marginal improvements over a simple random replacement strategy,
is empirically determined and depends on factors like the cache hit            underscoring their limited effectiveness for ANNS workloads.
ratio and coroutine switching overhead.                                            To address this, our key opportunity lies in exploiting access
Sync vs. Async execution. Panel (c) demonstrates the difference                skew by decoupling vertex-based records from page-based
between synchronous and asynchronous ANNS execution. In the                    organization. As illustrated in Fig.4, we measured the access fre-
synchronous model, when the CPU processes a query and encoun-                  quency of each vertex and page after executing a complete workload
ters a random I/O operation (e.g., accessing a vertex record not in            on the Sift1M and Gist1M datasets. We observe that while vertex-
memory) during graph traversal, a cache miss causes the CPU to                 level statistics reveal a significant access skew, this phenomenon is
stall until the record data is fetched from disk. In contrast, the asyn-       difficult to exploit with paging methods. Consequently, accessing
chronous model allows CPU to issue the request for the required                a single record necessitates loading its entire page into the cache,
vertex record and immediately switch to process another query                  even if other records on that page are infrequently accessed. This
coroutine managed by the scheduler. Each coroutine handles an                  reveals a pronounced locality mismatch in Sift1M: 47.3% of vertices
ANNS query and suspends execution upon an I/O operations. When                 remain unaccessed, yet only 0.1% of pages are untouched. A similar
the scheduler switches back to the previously suspended coroutine,             disparity is observed in Gist1M, undermining the effectiveness of
the CPU does not need to wait for the record as it has been loaded             traditional page-based cache replacement policies.
into the in-memory buffer pool. This overlapping between data                  Record-level Buffer Pool. Based on this observation, VeloANN
fetching and query computation sustains high CPU utilization and               adopts a record-level buffer pool which only caches the requested
significantly improves query throughput.                                       records and discards the co-resident records on the same page.
                                                                               However, achieving performance comparable to that of in-memory
                                                                               ANNS systems with a generic buffer pool is challenging for two
3.2    Buffer Pool Management                                                  main reasons. First, traditional buffer management techniques are
Paging methods, such as buffer pools or mmap, are fundamental to               ill-suited for graph-structured data; hash table-based methods incur
database systems but are often ill-suited for ANNS workloads. They             indirection overhead, while pointer swizzling cannot support graph
struggle to achieve high cache hit rates, given the unique and often           structures, as detailed in §2.4. Second, a fundamental granularity
unpredictable memory access patterns of ANNS queries. To demon-                mismatch exists between the record-based in-memory layout and
strate this, we experimented on the Sift1M dataset with a page-level           the page-oriented on-disk layout.
                                                                           5
    To address these challenges, VeloANN implements a slotted                                                                                       % D V H               ) U D J P H Q W
buffer pool to efficiently manage the ANNS graph layout. It allo-                                                  



                                                                                  3 H U F H Q W D J H    
cates an anonymous virtual memory region sized to a configured
fraction of the total index size (i.e., the buffer ratio) and partitions
it into slots, each sized to hold one vertex-based record. A free list                                             
tracks the available slots. To overcome the limitations of pointer
swizzling for graph structures, VeloANN employs a compact indi-
                                                                                                                    
rection array (the record mapping array in Fig.2). Because vertex                                                         6 L I W  0    * L V W  0           : L N L         , P D J H     7 H [ W
IDs are assigned contiguously, this array provides 𝑂 (1) mapping
from a vertex ID to a record’s physical location and centralizes
                                                                                Figure 6: Internal page fragmentation percentage of the on-
data-residency management, thereby resolving pointer swizzling’s
                                                                                disk layout for ANNS graph indexes.
incompatibility with graph structures. Each entry in this array is a
hybrid pointer in which the most significant bit encodes residency:
bit 1 indicates in-memory, with the remaining bits indexing a slot in
the buffer pool; bit 0 indicates on-disk, with the remaining bits en-           second chance. If the hand encounters an already Marked slot, it is
coding the page ID of the record’s location. This design eliminates             selected for eviction. This process continues until a target number
access overhead for memory-resident records.                                    of slots are freed, ensuring that frequently accessed records (e.g.,
    This buffer pool design requires a synchronization mechanism to             entry points) remain resident with minimal overhead. The eviction
manage concurrent operations (e.g., loading and eviction) in its slots.         process is designed as a scalable, concurrent coroutine to minimize
To this end, we explicitly maintain a state for each slot, managed via          contention with foreground query processing.
atomic operations. Since the number of slots is a subset of the total
vertex count, the memory footprint of this state management is                  3.3                              Physical Index Layout
minimal. After startup, all slots are initialized to the Free state, and        ANNS graph indexes typically employ a vertex-based layout, storing
subsequent record access operations check the corresponding state               each vertex’s vector and adjacency list together as a single record.
entry before proceeding. We now introduce the basic operations of               This layout leverages spatial locality in graph traversal, as a ver-
the buffer pool to illustrate the state transitions of a slot, as shown         tex’s vector and its out-neighbor list are typically accessed together
in the state diagram in Fig.5:                                                  during search. Each record is packed into fixed-size disk pages. To
                                                                                ensure uniform record sizes for efficient packing and lookup, each
                                                                                record’s adjacency list is padded to a fixed bound 𝑅. However, this
                evicting              evicting                                  layout incurs internal page fragmentation: fixed-size records often
                            Locked               Marked
                                                                                leave pages partially utilized, wasting space. As illustrated in Fig. 6,
                                         unmark      mark                       fragmentation remains low at small vector dimensionalities (e.g.,
                                                                                𝑑 = 128 for Sift1M). By contrast, as dimensionality increases, the
           Free             loading              Occupied                       upper bound on internal fragmentation rises; e.g., Gist1M reaches
                                                                                up to 52%, while other high-dimensional datasets are around 25%.
                                                                                Large, fixed-size records induce padding, leading to partial page uti-
        Figure 5: The state transitions of record states.                       lization. These observations motivate compressing vertex records
                                                                                and adopting a more compact on-disk layout to improve page pack-
Loading. The loading process is initiated when the record mapping               ing efficiency and reduce internal fragmentation.
array indicates that a requested vertex record resides on disk. The             Compressed Vertex-Based Record. We minimize record size by
buffer manager first acquires a slot from the free list; if none is             compressing both the vector and adjacency list of each vertex record.
available, an eviction is triggered. Once a slot is secured, its state is       Vectors are hierarchically compressed (e.g., ExtRaBitQ [12, 13]) into
atomically set to Locked via a compare-and-swap (CAS) operation                 (i) a 1-bit-per-dimension binary code kept in memory for fast ap-
to prevent race conditions. An I/O request is then dispatched to                proximate distances and (ii) an extended code (e.g., 4 bits per dimen-
fetch the required page from disk. Upon I/O completion, the target              sion) for accurate refinement. Prior work [12] shows that a 1-bit
record is parsed from the page, loaded into the slot, and the slot’s            binary plus 4-bit extended encoding balances search accuracy and
state is updated to Occupied.                                                   storage footprint; we adopt this default. Adjacency lists are sorted
Eviction. When the buffer pool approaches full, an eviction corou-              and integer-compressed (e.g., delta encoding or Partitioned Elias-
tine is invoked to free slots for new records. As established in our            Fano [38]) to reduce space consumption. The resulting variable-size
analysis (Table 1), conventional replacement policies are ill-suited            record (i.e., extended code plus compressed variable-size adjacency
for ANNS workloads; consequently, our design prioritizes low evic-              list) achieves an effective trade-off among storage footprint, search
tion overhead over cache effectiveness. To this end, we implement               performance, and accuracy.
a clock-based replacement policy with a second-chance mechanism.                Page Layout. To efficiently manage variable-size records on disk,
This policy employs a clock hand that sweeps over the buffer pool               we employ a B-tree-inspired slotted page layout, as illustrated in
slots in a circular manner. When the hand encounters a slot in the              Fig.7. Each page is partitioned into a header, a slot array, and a data
Occupied state, it transitions it to Marked and advances. If a query            heap. The 5-byte page header provides page-management meta-
accesses a Marked slot, its state reverts to Occupied, giving it a              data, including a slot count (Count) and heap pointers (HeapStart,
                                                                            6
             VID             Color         Length          StartOﬀset   Compressed      Compressed         Algorithm 1 Affine-based Record Identification
            4 byte                          2 byte            2 byte
                                                                          Vector          Adj List
                            1 byte

                                     8 byte total                         Variable-sized record                1: Input: graph 𝐺 (𝑃, 𝐸), build parameter 𝑙𝑏 , graph degree 𝑅, prun-
                                                                                                                    ing parameter 𝛼, affinity threshold 𝜏, affine size 𝑘
  Page Header               Record Meta 1             Record Meta 2       Record 2          Record 1
                                                                                                               2: Output: Graph 𝐺 (𝑃 ′ , 𝐸 ′ ) and affine record dictionary 𝑆
                     6 byte total
                                                                                                               3: 𝑆 ← {}                             ⊲ Initialize empty dictionary
    1 byte                1 byte             2 byte          2 byte                                            4: for each 𝑝 in 𝑃 do                ⊲ Graph construction process
      Tag                 Count           HeapStart        HeapUsed
                                                                                                               5:       [𝑉 , 𝐷] ← GreedySearch(𝐺, 𝑝, 𝑙𝑏 )
                                                                                                               6:      𝐴𝑝 ← ∅                         ⊲ Affine vertices for node 𝑝
Figure 7: On-disk paging layout for organizing compressed
                                                                                                               7:      for each (𝑣, 𝑑) in (𝑉 , 𝐷) do
variable-sized vertex-based records.
                                                                                                               8:          if 𝑑 ≤𝜏 and |𝐴𝑝 | < 𝑘 then
                                                                                                               9:              𝐴𝑝 ← 𝐴𝑝 ∪ {𝑣 }
HeapUsed). The slot array of fixed-size entries grows from the                                             10:         𝑆 [𝑝] ← 𝐴𝑝                   ⊲ Store affine vertices for node 𝑝
start of the page, while the data heap storing records grows back-
ward from the end; this opposing growth enables dynamic space                                              11:    ▷ Pruning mechanisms in proximity graph construction
sharing and minimizes internal fragmentation. Each 9-byte slot                                             12:    𝑁𝑜𝑢𝑡 (𝑝) ← PruneEdges(𝐺, 𝑝, 𝑉 , 𝑅, 𝛼)
contains the record’s 4-byte vertex ID (VID), a 1-byte Color tag to                                        13:    for each 𝑣 in 𝑁𝑜𝑢𝑡 (𝑝) do
differentiate vertex clusters (detailed in §3.4), the record’s 2-byte                                      14:        𝑁𝑜𝑢𝑡 (𝑣) ← 𝑁𝑜𝑢𝑡 (𝑣) ∪ {𝑝}
Length, and its 2-byte StartOffset within the heap. The slot array                                         15:        if |𝑁𝑜𝑢𝑡 (𝑣)| > 𝑅 then
is kept sorted by VID to enable fast binary-search lookups. This                                           16:            𝑁𝑜𝑢𝑡 (𝑣) ← RobustPrune(𝐺, 𝑣, 𝑁𝑜𝑢𝑡 (𝑣), 𝑅, 𝛼)
design achieves dense packing of variable-size records and fast                                            17: return 𝐺 (𝑃 ′ , 𝐸 ′ ), 𝑆
retrieval. The retrieval process for an on-disk vertex record begins
with a binary search on the sorted slot array to locate the metadata
entry for a given vertex ID 𝑣. The StartOffset and Length fields                                           into the vector quantization stage, as many vector compression
in this entry pinpoint the record’s location and size within the data                                      methods (e.g., PQ [24] or RabitQ [13]) require a clustering step.
heap. The system then reads the complete byte sequence, which                                                 However, naively identifying affine records in a graph with |𝑃 |
constitutes the vertex-based record, and uses it for subsequent                                            vertices requires 𝑂 (|𝑃 | 2 ) pairwise checks, which is prohibitive at
distance refinement and graph traversal.                                                                   scale. To address this, we integrate affinity identification into greedy
                                                                                                           proximity-graph construction with negligible additional overhead
3.4         Record Co-Placement Mechanism                                                                  by reusing the candidate sets and distance computations produced
Record-level buffering improves cache utilization by loading only                                          during neighbor selection. As shown in Algorithm 1 (lines 6–10),
the requested record, but may introduce I/O inefficiency because                                           for each vertex 𝑝 we select up to 𝑘 nearest candidates within the
page co-resident data is not retained. Consider a traversal path                                           threshold 𝜏 and store them in 𝑆 [𝑝], avoiding an additional pass
{𝑣 1 →𝑣 2 →𝑣 3 } with 𝑣 1 and 𝑣 3 on page 𝑃1 and 𝑣 2 on 𝑃2 , yielding the                                  over the dataset. Because the distance list 𝐷 is already computed
page access sequence {𝑃1 → 𝑃 2 → 𝑃 1 }. Fetching 𝑃 1 for 𝑣 1 reads 𝑣 3                                     for neighbor selection, affinity extraction reduces to per-vertex fil-
but discards it; when 𝑣 3 is later needed, 𝑃1 is fetched again, incurring                                  tering. We set the affinity bound 𝑘 relative to page capacity (i.e., the
redundant disk I/O. To mitigate this, VeloANN uses affinity-based                                          maximum number of records per page) to prevent affinity groups
record co-placement to improve page-level locality, loading affine                                         from spanning multiple pages.
records together while discarding non-affine ones.                                                         Affinity-based Record Co-Placement. We co-locate the affine
Affinity-based Record Identification. Unlike block shuffling [44],                                         records by iterating over the affinity dictionary and placing the sets
which co-locates records of adjacent vertices within the same page,                                        contiguously on disk. Within each page, we tag record slots with
our approach exploits the empirical observation that spatially prox-                                       Color (see Fig. 7) to distinguish sets: each set receives a unique non-
imate vectors are frequently co-traversed along search trajectories.                                       zero value, incremented cyclically; 0 denotes non-affine records.
This distinction is critical because modern proximity graphs (e.g.,                                        One byte suffices for this tag because pages typically host few sets.
Vamana [23]) optimize navigational efficiency rather than strictly                                         Pages are filled greedily. If a set does not fit in the remaining space,
preserving metric neighborhoods, employing long-range links and                                            we first pad the residual space with non-affine records (i.e., records
aggressively pruning edges among nearby vertices. Consequently,                                            not belonging to any affinity set). If none are available, we split the
adjacency-based co-location tends to pollute pages with records                                            set across page boundaries to avoid the complexity and overhead
that are geometrically distant, degrading locality. Formally, let 𝑑 (·, ·)                                 of more sophisticated placement algorithms. Upon accessing any
denote the distance metric. Two vectors 𝑣𝑖 and 𝑣 𝑗 are deemed affine                                       record with a non-zero Color tag, all co-tagged records on the page
iff 𝑑 (𝑣𝑖 , 𝑣 𝑗 ) ≤𝜏 for a given threshold 𝜏. We co-locate records whose                                   are proactively fetched into the buffer pool.
vectors are affine on the same page. This strategy enables a single
page read to serve multiple subsequent accesses, thereby reducing                                          4        CACHE-AWARE ANNS PROCEDURE
I/O operations. The distance threshold 𝜏 is selected heuristically                                         VeloANN employs a coroutine-based asynchronous execution model,
from dataset statistics. We determine this threshold through vector                                        overlapping computation with I/O to keep the CPU saturated. It
clustering, setting 𝜏 to the average of the 5th-percentile distance-to-                                    achieves QPS comparable to fully in-memory ANNS systems, but
centroid values across all clusters. This procedure can be integrated                                      single-query end-to-end latency remains higher due to disk I/O. This
                                                                                                       7
discrepancy arises from I/O stalls on the query’s critical execution          Algorithm 2 Cache-aware Beam Search in VeloANN.
path: while the system as a whole is asynchronous, a single                         Input: query 𝑞, candidate list size 𝐿, beam width 𝐵.
query’s critical path is not; its coroutine blocks until its own I/O           1: procedure VeloSearch(𝑞, 𝐿, 𝐵)
completes, making disk access effectively synchronous from that                2:      Candidate list 𝑝 ← entry points
query’s perspective.                                                           3:      Explored set 𝐸 ← ∅
   Closing the latency gap hinges on reducing I/O stalls on each               4:      while 𝑃 ⊈ 𝐸 do
query’s critical path by improving the cache hit rate for requested            5:         𝑣 ← top-1 nearest candidate to 𝑞 from 𝑃 \ 𝐸
vertex records. However, ANNS workloads exhibit irregular mem-
ory access and poor cache locality. To address this, VeloANN intro-            6:          𝐶 ← top-𝐵 nearest candidates to𝑞 from 𝑃 \ 𝐸
duces two complementary optimizations: proactive prefetching and               7:          ⊲ Prioritize the in-memory candidate when 𝑣 is on disk
cache-aware beam search. Proactive prefetching (§4.1) issues asyn-             8:          if InOnDisk(𝑣) then
chronous I/O for promising candidates ahead of use. Cache-aware                9:              for each 𝑐 in 𝐶 do
beam search (§4.2) alters the best-first search strategy to opportunis-       10:                  if InMemory(𝑐) then
tically prioritize exploring candidates already in memory, even if            11:                       𝑣 ← 𝑐; break
they are slightly sub-optimal. Together, these techniques minimize            12:                  else
critical-path stalls and effectively hide I/O latency.                        13:                       prefetch(𝑐) ⊲ Prefetch on-disk candidate 𝑐
                                                                              14:             𝑣 = read(𝑣).await ⊲ Suspend execution if 𝑣 is on disk
4.1    Prefetching                                                            15:             for each nbr in 𝑣.neighbors \ 𝐸 do
Our prefetching strategy is based on a key insight into the behavior          16:                 𝑑𝑖𝑠 ← estimate_distance(nbr, 𝑞)
of best-first graph traversal: the vertices to be explored in the imme-       17:                 𝑃 .insert(⟨nbr, 𝑑𝑖𝑠⟩)
diate future are highly likely to be among the top-ranked candidates          18:             𝐸.insert(𝑣.neighbors)
in the current search frontier. This predictability stems from the            19:             Trim candidate set 𝑃 to size 𝐿
best-first search algorithm’s iterative nature, where it consistently         20:          return top-𝑘 nearest vectors to 𝑞 in 𝐸
expands the search frontier by selecting the most promising vertices
based on their proximity to the query.
   To capitalize on this predictability, VeloANN employs a prefetch-          in-memory candidate increases proportionally with the beam size.
ing mechanism that preloads the records of the top-𝐵 candidates               Second, it minimizes I/O stalls by prioritizing the exploration of
in each search iteration. unlike hardware prefetching, which is               in-memory candidates while asynchronously prefetching other
limited to in-memory data, our prefetching approach targets on-               candidates in the background, effectively hiding I/O latency.
disk records. VeloANN implements this via a dedicated prefetching                Algorithm 2 illustrates our cache-aware search procedure with
coroutine that runs concurrently with the main search process. The            beam width 𝐵, which deviates from a standard greedy beam search
prefetching procedure reuses the asynchronous loading methods                 by opportunistically prioritizing in-memory candidates. In each
of the buffer pool manager. As illustrated in Fig.3, the prefetch-            iteration, the algorithm first identifies the top candidate 𝑣 from the
ing coroutine identifies the top-𝐵 most promising candidates in               candidate list 𝑃 (line 6) and constructs a look-ahead set 𝐶 contain-
each search iteration and issues asynchronous I/O requests to load            ing the top-𝐵 candidates (line 7). If 𝑣 resides on disk (line 9), the
their records into the buffer pool. Consequently, when the search             algorithm attempts an opportunistic pivot by iterating through the
algorithm proceeds to explore these candidates, their data is likely          look-ahead set 𝐶. It selects the first available in-memory candidate
already present in memory, significantly improving the cache hit              𝑐 for immediate exploration (lines 10–12), allowing computation to
ratio and minimizing stalls on the query’s critical path.                     proceed without stalling. For any on-disk candidates encountered
                                                                              in 𝐶, it issues asynchronous prefetch requests (line 14). If no in-
4.2    Cache-aware Beam Search                                                memory candidate is found within the look-ahead set, the search
                                                                              defaults to processing 𝑣, potentially suspending execution until its
The best-first search procedure creates a strong dependency be-               data is retrieved from disk (line 17). After a vertex is explored, its
tween I/O and computation, as the algorithm must wait for candi-              unvisited neighbors are added to the candidate list, and the vertex
date records to be fetched from disk before proceeding. To mitigate           is marked as explored (lines 18–22). Finally, the candidate list is
this, beam search, as proposed by DiskANN [23], explores a batch              trimmed to a maximum size of 𝐿 (line 22). This design effectively
of 𝐵 vertex candidates concurrently instead of a single nearest ver-          hides I/O latency by combining the opportunistic exploration of in-
tex. However, this approach remains constrained by a sequential               memory candidates with asynchronous data prefetching, thereby
compute-I/O pattern and is ultimately bottlenecked by the slowest             reducing I/O stalls and overall query latency.
I/O operation in each batch.
   To address this, we introduce a cache-aware beam search that               5 EVALUATION
leverages VeloANN’s efficient buffer pool. Unlike standard beam
search, which synchronously issues a batch of I/O requests, our               5.1 System Implementation
approach prioritizes the exploration of candidates that are cached            We implemented VeloANN in ∼13k lines of Rust code, chosen for its
in memory, while asynchronously prefetching those on disk. This               powerful asynchronous programming features and strong safety
strategy yields two key advantages. First, it amplifies the effective         guarantees. The query engine of VeloANN adopts a thread-per-core
cache hit rate by a factor of 𝐵, as the probability of finding an             asynchronous execution model, where each thread runs a dedicated
                                                                          8
                                                                                                                      9 H O R $ 1 1                 6 W D U O L Q J                   3 L S H $ 1 1             ' L V N $ 1 1
                                                                       * L V W                                                   : L N L                                                                      , P D J H                                                  7 H [ W
                                                                                                                                                                      
                                                                                                                                                                                                                                              


 7 K S X W    4 3 6 
                                                                                                                                                                              
                                                                                                                                                                                                                                      
                                                                                                                                                                              
                                                                                                                                                                                                                                        
                                                                                                                                                                               
                                                                                                                                                                                                                                         
                                                                                                                                                                               

                                               


                    / D W H Q F \   P V 
                                                                                                                                                                                                                                              
                                                                                                                                                                              
                                                                                                                                                                                                                                                
                                                                                                                                                                              
                                                                                                                                                                                                                                                                
                                                              5 H F D O O #                                      5 H F D O O #                                                             5 H F D O O #                                   5 H F D O O #      

                      Figure 8: Performance comparison of VeloANN with DiskANN, Starling, and PipeANN on four high-dimensional datasets.

                                                             Table 2: Statistics of the datasets.                                                                            and 10K query vectors, while Text comprises 768-dimensional text
                                                                                                                                                                             embeddings with a 100M-vector base set and 10K query vectors.
               Dataset                                     # of vectors               Dimension             Size (GB)         # of queries                                   Compared Systems. We benchmark VeloANN against three state-
               Sift1M                                           1M                       128                   0.52              10,000                                      of-the-art disk-based ANNS systems: DiskANN [23], Starling [44],
               GIST1M                                           1M                       960                   1.28              1,000                                       and PipeANN [15]. DiskANN serves as a widely adopted baseline for
               Wiki                                            35M                       768                   101               10,000                                      disk-based ANNS system. Starling enhances search locality through
               Image                                          100M                       512                  191.7              10,000                                      on-disk graph layout reordering and employs an in-memory index
               Text                                           100M                       768                  286.4              10,000                                      to reduce I/O traversal paths. PipeANN accelerates on-disk search
                                                                                                                                                                             by relaxing strict best-first ordering to parallelize I/O requests and
                                                                                                                                                                             search procedures. To ensure a fair comparison, we standardize
                                                                                                                                                                             the experimental setup by using RabitQ [13] for in-memory vector
  scheduler to minimize context-switching overhead and enhance
                                                                                                                                                                             compression across all systems and replacing AIO with io_uring
  cache locality. For high-performance asynchronous I/O operations,
                                                                                                                                                                             in DiskANN and Starling. We enable the O_DIRECT option for all
  we implement the asynchronous runtime based on io_uring, with
                                                                                                                                                                             systems to bypass the operating system page cache. Since all sys-
  each thread employing a private instance to issue I/O requests inde-
                                                                                                                                                                             tems utilize the Vamana [23] graph index, we employ identical
  pendently. To reduce memory access overhead, the buffer manager
                                                                                                                                                                             construction hyperparameters when building the graphs.
  allocates its memory region using mmap with huge pages. VeloANN
                                                                                                                                                                                To ensure a fair comparison, we set the buffer ratio (i.e., mem-
  employs ExtRabitQ [12, 13] for vector compression and the Par-
                                                                                                                                                                             ory budget) for each system to 20% of its respective disk index
  titioned Elias-Fano coding [38] for compressing adjacency lists,
                                                                                                                                                                             size. This normalization prevents VeloANN, which has smaller on-
  creating a compact graph layout that is tightly integrated into the
                                                                                                                                                                             disk compressed indexes, from receiving a disproportionately large
  query engine. Rust’s compile-time checks provide strong guaran-
                                                                                                                                                                             buffer pool allocation. We disable the SQ_POLL feature of io_uring
  tees for memory and concurrency safety throughout the system.
                                                                                                                                                                             because it introduces an additional thread to busy-poll I/O comple-
                                                                                                                                                                             tion events, which would compromise fairness in the comparsion.
   5.2                                             Experimental Setup                                                                                                        Furthermore, we observe that for high-dimensional datasets (e.g.,
  Testbed. We conduct our evaluation on a server equipped with a                                                                                                             Gist1M with 960 dimensions), individual vertex records can occupy
  48-core Intel(R) Xeon(R) Platinum 8457C processor operating at                                                                                                             an entire data page (default 4 KB). To address this issue, we con-
  2.00GHz, 384GB of RAM configured as 24×16GB DDR4 modules                                                                                                                   figure baseline systems with an 8 KB page size for these datasets,
  running at 2933MT/s, and two 3.84TB Solidigm NVMe SSDs. The                                                                                                                while VeloANN employs a 4 KB page size.
  system runs Ubuntu 24.04 LTS with Linux kernel version 6.8.0.                                                                                                              Evaluation Metrics. We evaluate and report the following key
  Datasets. Our evaluation employs five representative benchmark                                                                                                             metrics: Recall@10 represents the fraction of the true 10 nearest
  datasets spanning diverse dimensions and scales, as detailed in Ta-                                                                                                        neighbors retrieved in the top-10 results, measuring the quality of
  ble 2. These include two commonly used Sift1M [25] and Gist1M [25]                                                                                                         the search results. Query latency indicates the average per-query
  datasets, for which we utilize the provided base and query sets for                                                                                                        processing time in millisecond (ms). Throughput refers to the num-
  micro-level analysis. Additionally, we incorporate three datasets                                                                                                          ber of queries processed per second under concurrent load (QPS).
  constructed from high-dimensional embeddings generated by ad-                                                                                                              The number of disk I/Os indicates the average number of I/O re-
  vanced deep learning models. The first is a 512-dimensional text                                                                                                           quests issued per query.
  dataset created using the Cohere [21] embedding model, with 35M
  base vectors generated from Wikipedia and 10K query vectors sam-
  pled from the MMLU [20] dataset. The second and third datasets                                                                                                              5.3              Overall Performance
  are 100M-scale high-dimensional embeddings. Image consists of                                                                                                               Query Throughput and Latency. As illustrated in Fig.8, we com-
  512-dimensional image embeddings with a 100M-vector base set                                                                                                                pare the query throughput and latency of VeloANN against three
                                                                                                                                                                      9
Table 3: Comparison of index size and memory footprint                                                                             6 L I W      G                 7 H [ W      G                                      * L V W      G 
across different ANN systems. The Origin row shows the size



                                                                                         1 R U P   7 K U R X J K S X W                                                     1 R U P   / D W H Q F \
of the raw vector datasets for reference.                                                                                                                                                                
                                                                                                                            
                     Disk (GB)          Memory Footprint (GB)                                                                                                                                             
               Gist Wiki Image Text Gist Wiki Image          Text                                                           
                                                                                                                                                                                                                                                    
    Origin  4.2       96    286    201  –    –        –       –                                                                             % D W F K  6 L ] H                                                         % D W F K  6 L ] H
    DiskANN 8.0      135    382    286 0.87 24.5     49.7    38.6
    VeloANN 0.8      21     64     45 0.30 7.3       20.8    15.7
                                                                               Figure 9: Query throughput and latency of VeloANN with vary-
                                                                               ing batch size (B) settings on three datasets.
baselines across four high-dimensional datasets. The results demon-
strate that VeloANN consistently outperforms all baselines in both
query throughput and latency at the same recall@10. VeloANN’s
                                                                                                                                                :                         :                                            :     
                                                                                                                                                :                         :                                            :      
                                                                                                                      


                                                                                7 K U R X J K S X W   4 3 6 
asynchronous execution model orchestrates multiple query re-
                                                                                                                                                                                    

                                                                                                                                                                                   / D W H Q F \   P V 
                                                                                                                                                                                        
quests to overlap I/O and computation, yielding significant through-
put gains. This results in a query throughput 1.4× to 4.6× higher                                                                                                                   
than PipeANN. Furthermore, by leveraging prefetching and a cache-                                                                                                                    
aware beam search, VeloANN effectively mitigates I/O latency from                                                                                                                    
                                                                                                                       
                                                                                                                                                                                                                                
the critical execution path, achieving 1.6× lower latency than the
latency-optimized PipeANN at high recall. For instance, on the                                                                             5 H F D O O #                                                          5 H F D O O #      
Wikipedia dataset, VeloANN delivers over 1k QPS at 97% recall, sur-
passing PipeANN, Starling, and DiskANN by 3.3×, 7.3×, and 5.8×,
respectively. In terms of query latency, VeloANN also achieves 1.44×,          Figure 10: Query throughput and latency of VeloANN under
3.25× and 2.5× lower latency than DiskANN, Starling, and PipeANN,              different beam width (W) settings on the Image dataset.
respectively. We also observe that Starling’s performance is compa-
rable to DiskANN’s, despite its larger 8 KB page size setting. This is
because an 8 KB page accommodates only a few high-dimensional                  buffer manager. While we set the buffer pool to 20% of the on-disk
records (e.g., three 512D or one 768D vector), which diminishes the            index size in our experiments, this ratio is a flexible parameter for
benefit of its block shuffling layout and underscores the importance           tuning performance and memory trade-offs. The metadata, which
of a compressed index for high-dimensional ANNS.                               includes record mappings and states, adds only a minor overhead
Index Size and Memory Footprint. As the three baseline sys-                    of approximately 1% of the disk index size.
tems share the same underlying storage layout (differing only in
ordering for Starling), we compare the index size and memory foot-             5.4                                          Micro Analysis
print of VeloANN with those of DiskANN as illustrated in Table 3.              Batch Size. As described in §3.1, the scheduler’s batch size (𝐵)
The result shows that VeloANN’s disk consumption is up to 10×                  creates a trade-off between throughput and latency. A larger batch
smaller than DiskANN. This significant space saving is achieved                size can improve throughput by more effectively hiding I/O latency,
through a compressed index layout that quantizes high-dimensional              as the scheduler can execute other queries during I/O stalls. Con-
vectors to 4 bits per dimension and compresses each vertex’s adja-             versely, a larger batch may increase average query latency due to
cency list with a sorted integer encoding scheme. These optimiza-              computational and scheduling overhead. Fig.9 illustrates this be-
tions yield a nearly 4.5× compression ratio compared to the original           havior, showing that query latency grows approximately linearly
raw vectors (labeled as Origin). In contrast, baseline systems exhibit         as 𝐵 increases. The impact on throughput, however, is dataset-
significant space amplification. This is because, in addition to graph         dependent. On the lower-dimensional Sift1M dataset, throughput
structure overhead, the DiskANN-style layout suffers from severe               scales effectively with larger batch sizes. This is because its low
internal page fragmentation especially for high-dimensional vector             computational cost allows the scheduler to mask I/O wait times
data. For instance, on the Image dataset, a standard 4 KB page ac-             by executing more concurrent queries. In contrast, on the high-
commodates only one record—a 2,048-byte vector and a 256-byte                  dimensional Gist1M and Image datasets, throughput gains diminish
adjacency list for 64 out-degrees—wasting 1,792 bytes per page. By             beyond 𝐵 = 2. For these datasets, the high computational cost satu-
mitigating this fragmentation, VeloANN’s compact encoding scheme               rates the CPU even with a small batch, making larger batches less
enables multiple records to be stored per page, demonstrating its              effective. Based on these findings, we recommend a default batch
effectiveness for managing high-dimensional datasets.                          size of 𝐵 = 2 for high-dimensional datasets, as it provides a strong
   As detailed in Table 3, VeloANN also achieves the lowest memory             balance between throughput and latency.
footprint, requiring up to 2.9× less memory than that of DiskANN.              Beam Width. The beam width (𝑊 ) in our cache-aware ANNS
This footprint comprises three components: highly-compressed                   procedure presents a trade-off between I/O latency hiding and I/O
(1-bit per dimension) vectors for initial distance estimation, a buffer        efficiency. Increasing the beam width expands the set of candidates
pool for caching vertex records, and auxiliary metadata for the                for exploration, which improves the likelihood of cache hits and
                                                                          10
                                                                                                                                                                                                                                                                               9 H O R $ 1 1                                       9 H O R $ 1 1         
                                                                                                                                                                                                                                                                               9 H O R $ 1 1                                       9 H O R $ 1 1  3 D J H
                                                                  ' L V N $ 1 1                                                3 L S H $ 1 1


    7 K S X W    4 3 6 
                                                                                                                                                                                                                                                                                                                                     


                                                                                                                                                                                                              4 3 6   T X H U L H V  V H F 
                                         N                      6 W D U O L Q J                                              9 H O R $ 1 1                                                                                                          

                                                                                                                                                                                                                                                                                                                ' L V N  ,  2
                                                                                                                                                                                                                                                                                                                                 
                                           N                                                                                                                                                                                                         
                                                                                                                                                                                                                                                                                                                                     
                                           N                                                                                                                                                                                                                                                                                                                              
                                                                                                                                                                                                                                                             % X I I H U  5 D W L R                                                         % X I I H U  5 D W L R    
                                                                                         1 X P E H U  R I  7 K U H D G V
                                                                                                                                                                                                            Figure 13: Query latency and disk I/O count under different
Figure 11: Query throughput of DiskANN, Staring, PipeANN                                                                                                                                                    distance threshold 𝜏 settings on the Sift1M dataset.
and VeloANN with varying thread number settings on the
Wikipedia dataset.

                                        9 H O R $ 1 1                       9 H O R $ 1 1                                               9 H O R $ 1 1                        9 D P D Q D        Comparing to In-Memory ANNS Indexes. Fig.12 illustrates
                                                                                                                                                                                                            the performance of VeloANN on the Image dataset with varying
                                                                                                                             


  7 K U R X J K S X W   4 3 6 
                                                                                                                                                                                                            buffer ratios, compared to a fully in-memory Vamana index (i.e.,

                                                                                                    / D W H Q F \    V 
                                                                                                                                                                                                    a 100% buffer ratio). By leveraging its asynchronous execution
                                                                                                                                                                                                            model, VeloANN effectively saturates the CPU with computational
                                                                                                                             
                                                                                                                                                                                                            tasks, avoiding idle periods typically spent waiting for I/O comple-
                                                                                                                                  
                                                                                                                                                                                                tion. Furthermore, its prefetching and cache-aware beam search
                                                            5 H F D O O #                                                                            5 H F D O O #                              strategies significantly reduce I/O stalls in the critical query path,
                                                                                                                                                                                                            effectively hiding much of the disk I/O latency. As a result of these
Figure 12: Query throughput and latency of VeloANN on the                                                                                                                                                   optimizations, VeloANN achieves throughput comparable to the in-
Image dataset with varying buffer ratios, compared to a fully                                                                                                                                               memory Vamana index. Specifically, at 95% Recall@10, it delivers
in-memory Vamana index.                                                                                                                                                                                     0.73×, 0.78×, and 0.92× the QPS of Vamana with buffer ratios of
                                                                                                                                                                                                            10%, 30%, and 50%, respectively. Correspondingly, at the same re-
                                                                                                                                                                                                            call target, the query latencies are 2.23×, 2.19×, and 1.86× higher
                                                                                                                                                                                                            than Vamana. These results demonstrate that VeloANN offers a
allows the scheduler to hide I/O latency by processing other candi-                                                                                                                                         cost-effective trade-off, achieving, for instance, 78% of in-memory
dates. However, this also leads to wasted I/O bandwidth compared                                                                                                                                            Vamana’s throughput while using only 30% of the index size in
to a best-first search and extra prefetching coroutine overhead, as                                                                                                                                         memory (equivalent to 7% of the original dataset size).
the algorithm can prefetch suboptimal vertices that are ultimately                                                                                                                                          Record Co-Placement with Varying Threshold 𝜏. The dis-
pruned from the final search path. Fig.10 illustrates this trade-off                                                                                                                                        tance threshold 𝜏 is crucial for record co-placement mechanism. We
on the Image dataset, showing how query throughput and latency                                                                                                                                              evaluate its effect by comparing VeloANN with different 𝜏 settings
vary with different beam widths. As shown, performance peaks at                                                                                                                                             against VeloANN-Page, a baseline that loads all records into the
𝑊 = 4, after which it begins to decline. Interestingly, a beam width                                                                                                                                        buffer pool from a page. As illustrated in Fig.13, the page-based ap-
of 𝑊 = 1 results in worse performance than a standard best-first                                                                                                                                            proach (VeloANN-Page) performs the worst, exhibiting the highest
search (𝑊 = 0). This occurs because the time required to prefetch                                                                                                                                           disk I/O count and query latency. This is because loading entire
a single vertex from disk is greater than the processing time for                                                                                                                                           pages pollutes the buffer with unnecessary records, evicting more
an in-memory vertex. Consequently, the search process frequently                                                                                                                                            valuable data. In contrast, VeloANN with 𝜏 = 0% fetches only a
stalls while waiting for the I/O to complete, rendering the single-                                                                                                                                         single record, avoiding pollution but missing I/O consolidation op-
vertex prefetching ineffective. Therefore, selecting an appropriate                                                                                                                                         portunities. Setting 𝜏 = 5% strikes an optimal balance, achieving
beam width is crucial for achieving optimal performance.                                                                                                                                                    the lowest query latency and disk I/O, up to 1.28× lower than the
Varying Thread Number. Fig.11 evaluates the throughput scal-                                                                                                                                                𝜏 = 0% case. However, increasing the threshold further to 𝜏 = 10%
ability of VeloANN against its baselines as the number of concur-                                                                                                                                           degrades performance. A larger 𝜏 relaxes the affinity requirement
rent worker threads increases from 1 to 32. Each system is con-                                                                                                                                             for co-placement, reducing the effectiveness of I/O consolidation
figured to achieve approximately 95% recall@10. We employ a                                                                                                                                                 and leading to worse performance.
round-robin strategy to distribute queries evenly among the threads.
VeloANN demonstrates superior scalability, consistently outperform-
ing all baselines by a significant margin. VeloANN employs an ef-                                                                                                                                           5.5                                       Breakdown Analysis
fective record-level buffer pool to reduce disk I/Os and leverages                                                                                                                                          In this section, we analyze the performance gap between Baseline
coroutine-based execution to enhance query throughput. At 32                                                                                                                                                and VeloANN by progressively enabling the proposed optimizations.
threads, VeloANN reaches a peak throughput of ∼14k QPS. This                                                                                                                                                Based on the index layout shuffled by record co-placement mecha-
represents a 1.39× speedup over PipeANN and more than a 1.82×                                                                                                                                               nism, we incrementally integrate the key techniques into Baseline
speedup over DiskANN and Starling. This superior scalability demon-                                                                                                                                         and evaluate query throughput and latency across target recall lev-
strates the VeloANN’s I/O efficiency.                                                                                                                                                                       els. We fix the memory buffer ratio at 10% in bellow experiments.
                                                                                                                                                                                                       11
                                            % D V H O L Q H                   $ V \ Q F        5 H F R U G                          3 U H I H W F K               & % 6        graph-based ANNS becomes a significant bottleneck, limiting their
                                                                                                                                                                                   scalability and applicability.
                                                                                                                        



     7 K U R X J K S X W   4 3 6 
                                                                                                                                                                                   On-Disk ANNS Storage. To cost-effectively support large-scale

                                                                                               / D W H Q F \    V 
                                                                                                                                                                               vector datasets, recent researches have focused on storing ANNS
                                                                                                                            
                                                                                                                                                                                indexes on disk. Cluster-based methods, such as SPANN [4] and
                                                                                                                                                                                   ScaNN [16], partition vectors into clusters that are stored on disk
                                                                                                                             
                                                                                                                                                                           and indexed by an in-memory graph. DiskANN [23], a pioneering
                                                                  5 H F D O O #                                                  5 H F D O O #                         on-disk graph-based system, stores compressed vectors in mem-
                                                                                                                                                                                   ory for I/O-free navigation and retrieves full-precision vectors
 Figure 14: Breakdown analysis of VeloANN on Text dataset.                                                                                                                         from disk only to refine search results. Subsequent works like
                                                                                                                                                                                   DiskANN++ [36] and Starling [44] introduce further optimizations,
                                                                                                                                                                                   including sampled in-memory indexes to shorten I/O traversal paths
                                                                                                                                                                                   and topology-guided data co-placement to enhance locality. Others
+Async. To address CPU underutilization, we introduce an asyn-
                                                                                                                                                                                   focus on layout optimization; for instance, Gorgeous [48] introduces
chronous execution model (§3.1) for Baseline. This variant over-
                                                                                                                                                                                   a memory cache that prioritizes the graph structure over vector data,
laps I/O with computation (e.g., distance comparison operations)
                                                                                                                                                                                   as the former is accessed more frequently. To improve update per-
to maximize CPU utilization, yielding 1.8× higher throughput at
                                                                                                                                                                                   formance, OdinANN [17] utilizes direct insertion into the on-disk
95% recall@10 than Baseline. However, frequent context switches
                                                                                                                                                                                   index, avoiding the costly batch-merge process of other systems. In
among query coroutines increase per-query latency, about 1.6×
                                                                                                                                                                                   contrast, VeloANN achieves superior I/O efficiency through a com-
higher at 95% recall@10.
                                                                                                                                                                                   bination of a compressed on-disk graph layout, memory-optimized
+Record. We integrate a record-level buffer pool (§3.2) that pro-
                                                                                                                                                                                   record caching, and an integrated layout shuffling process during
vides finer-grained memory management compared to page-level
                                                                                                                                                                                   graph construction that incurs negligible additional overhead.
caching. This optimization yields a query throughput improvement
                                                                                                                                                                                   I/O-Efficient ANNS Query Processing. To enhance I/O effi-
of 1.23× to 1.9× over the async variant and Baseline, respectively.
                                                                                                                                                                                   ciency in query processing, cluster-based index SPANN identify
Additionally, it achieves a 1.21× reduction in query latency rela-
                                                                                                                                                                                   the nearest clusters in memory and utilize batched I/O to amor-
tive to the async variant. These performance gains primarily result
                                                                                                                                                                                   tize latency. However, their coarse-grained nature, compared to
from reduced disk I/O operations enabled by record-level caching.
                                                                                                                                                                                   graph-based indexes, often results in lower search throughput.
The fine-grained caching mechanism avoids loading unnecessary
                                                                                                                                                                                   DiskANN [23] pioneered a beam search strategy that concurrently
records into memory, thereby preserving memory resources for
                                                                                                                                                                                   fetches multiple candidate vectors at each step to mitigate I/O stalls.
more frequently accessed record data.
                                                                                                                                                                                   This approach, however, is constrained by a sequential compute-
+Prefetch. To further mitigate disk I/O stalls caused by cache
                                                                                                                                                                                   I/O pattern, where performance is bottlenecked by the slowest I/O
misses, we introduce a prefetching strategy (§4) that performs stride
                                                                                                                                                                                   operation in each batch. To improve the I/O-efficiency, Starling [44]
prefetching of records for the next several vertices in the candidate
                                                                                                                                                                                   proposes a block search method that explores additional vertices
list. By anticipating future record requests based on the current
                                                                                                                                                                                   within the same data block for each disk I/O, exploiting the data
candidate list, this strategy proactively fetches the corresponding
                                                                                                                                                                                   locality from its shuffling strategy. PipeANN [15] further improves
records into the buffer pool. This approach significantly improves
                                                                                                                                                                                   upon this by pipelining computation and I/O, aligning its best-first
the cache hit rate and reduces I/O stalls in the critical query path.
                                                                                                                                                                                   search with modern SSD characteristics to enable effective overlap.
As shown in Fig.14, this method enhances query throughput by
                                                                                                                                                                                   In contrast, VeloANN introduces a fully asynchronous search proce-
2.15× at 95% recall@10 and decreases average query latency by
                                                                                                                                                                                   dure coupled with a record-level memory cache, which minimizes
1.34× compared to the async variant.
                                                                                                                                                                                   I/O stalls on the critical path and achieves performance comparable
+CBS (Cache-Aware Beam Search). Integrating the above opti-
                                                                                                                                                                                   even to fully in-memory indexes.
mizations, VeloANN further reduces latency with cache-aware beam
search. CBS opportunistically prioritizes in-memory candidates
within the beam, increasing cache hit rate and reducing critical-                                                                                                                  7    CONCLUSION
path I/O stalls. At 95% recall@10, CBS attains the lowest latency,                                                                                                                 We present VeloANN, a graph-based ANNS for workloads exceeding
1.5× lower than the async variant, and improves throughput by                                                                                                                      main-memory capacity. It couples an optimized on-disk layout with
1.5× over the async variant and 2.2× over Baseline. In terms of                                                                                                                    a coroutine-based asynchronous runtime to reduce storage stalls
query throughput, it achieves a 1.5× improvement over the async                                                                                                                    and improve CPU utilization. Built on a record-level buffer pool,
variant and a 2.2× improvement over the baseline at 95% recall@10.                                                                                                                 VeloANN integrates proactive prefetching and cache-aware beam
                                                                                                                                                                                   search to raise cache hit rates and overall performance. Experi-
6                              RELATED WORK                                                                                                                                        ments show that VeloANN outperforms state-of-the-art disk-based
ANNS index. Research on ANNS indexes is generally categorized                                                                                                                      baselines in throughput and latency, while approaching in-memory
into three types: cluster-based [2, 4, 7, 47], hashing-based [6, 11, 33,                                                                                                           performance with a smaller memory footprint.
43, 49], and graph-based [3, 10, 18, 34, 39, 45] indexes. Among these,
graph-based approaches stand out in large-scale, high-dimensional                                                                                                                  REFERENCES
search by offering both high speed and accuracy. However, as                                                                                                                        [1] 2025. A. Kane. pgvector (Open-source vector similarity search for Postgres).
dataset sizes continue to grow, the memory overhead of in-memory                                                                                                                        https://github.com/pgvector/pgvector Accessed: February 27, 2026.
                                                                                                                                                                              12
 [2] Fabien André, Anne-Marie Kermarrec, and Nicolas Le Scouarnec. 2016. Cache                 [27] Viktor Leis. 2024. LeanStore: A High-Performance Storage Engine for NVMe
     locality is not enough: High-performance nearest neighbor search with product                  SSDs. Proceedings of the VLDB Endowment 17, 12 (2024), 4536–4545.
     quantization fast scan. In 42nd International Conference on Very Large Data Bases,        [28] Viktor Leis, Adnan Alhomssi, Tobias Ziegler, Yannick Loeck, and Christian
     Vol. 9. 12.                                                                                    Dietrich. 2023. Virtual-memory assisted buffer management. Proceedings of the
 [3] Ilias Azizi, Karima Echihabi, and Themis Palpanas. 2023. Elpis: Graph-based                    ACM on Management of Data 1, 1 (2023), 1–25.
     similarity search for scalable data science. Proceedings of the VLDB Endowment            [29] Scott T Leutenegger and Daniel Dias. 1993. A modeling study of the TPC-C
     16, 6 (2023), 1548–1559.                                                                       benchmark. ACM Sigmod Record 22, 2 (1993), 22–31.
 [4] Qi Chen, Bing Zhao, Haidong Wang, Mingqin Li, Chuanjie Liu, Zengzhong                     [30] Patrick Lewis, Ethan Perez, Aleksandra Piktus, Fabio Petroni, Vladimir
     Li, Mao Yang, and Jingdong Wang. 2021. Spann: Highly-efficient billion-scale                   Karpukhin, Naman Goyal, Heinrich Küttler, Mike Lewis, Wen-tau Yih, Tim Rock-
     approximate nearest neighborhood search. Advances in Neural Information                        täschel, et al. 2020. Retrieval-augmented generation for knowledge-intensive nlp
     Processing Systems 34 (2021), 5199–5212.                                                       tasks. Advances in neural information processing systems 33 (2020), 9459–9474.
 [5] Andrew Crotty, Viktor Leis, and Andrew Pavlo. 2022. Are you sure you want to              [31] Jie Li, Haifeng Liu, Chuanghua Gui, Jianyu Chen, Zhenyuan Ni, Ning Wang, and
     use mmap in your database management system?. In CIDR.                                         Yuan Chen. 2018. The design and implementation of a real time visual search
 [6] Mayur Datar, Nicole Immorlica, Piotr Indyk, and Vahab S Mirrokni. 2004. Locality-              system on JD E-commerce platform. In Proceedings of the 19th International
     sensitive hashing scheme based on p-stable distributions. In Proceedings of the                Middleware Conference Industry. 9–16.
     twentieth annual symposium on Computational geometry. 253–262.                            [32] Sen Li, Fuyu Lv, Taiwei Jin, Guli Lin, Keping Yang, Xiaoyi Zeng, Xiao-Ming Wu,
 [7] Yihe Dong, Piotr Indyk, Ilya Razenshteyn, and Tal Wagner. 2019. Learning space                 and Qianli Ma. 2021. Embedding-based product retrieval in taobao search. In
     partitions for nearest neighbor search. arXiv preprint arXiv:1901.08544 (2019).                Proceedings of the 27th ACM SIGKDD Conference on Knowledge Discovery & Data
 [8] Cong Fu and Deng Cai. 2016. Efanna: An extremely fast approximate nearest                      Mining. 3181–3189.
     neighbor search algorithm based on knn graph. arXiv preprint arXiv:1609.07228             [33] Yingfan Liu, Jiangtao Cui, Zi Huang, Hui Li, and Heng Tao Shen. 2014. SK-LSH:
     (2016).                                                                                        an efficient index structure for approximate nearest neighbor search. Proceedings
 [9] Cong Fu, Changxu Wang, and Deng Cai. 2021. High dimensional similarity                         of the VLDB Endowment 7, 9 (2014), 745–756.
     search with satellite system graph: Efficiency, scalability, and unindexed query          [34] Yu A Malkov and Dmitry A Yashunin. 2018. Efficient and robust approximate
     compatibility. IEEE Transactions on Pattern Analysis and Machine Intelligence 44,              nearest neighbor search using hierarchical navigable small world graphs. IEEE
     8 (2021), 4139–4150.                                                                           transactions on pattern analysis and machine intelligence 42, 4 (2018), 824–836.
[10] Cong Fu, Chao Xiang, Changxu Wang, and Deng Cai. 2017. Fast approximate                   [35] Javier Vargas Munoz, Marcos A Gonçalves, Zanoni Dias, and Ricardo da S Torres.
     nearest neighbor search with the navigating spreading-out graph. arXiv preprint                2019. Hierarchical clustering-based graphs for large scale approximate nearest
     arXiv:1707.00143 (2017).                                                                       neighbor search. Pattern Recognition 96 (2019), 106970.
[11] Junhao Gan, Jianlin Feng, Qiong Fang, and Wilfred Ng. 2012. Locality-sensitive            [36] Jiongkang Ni, Xiaoliang Xu, Yuxiang Wang, Can Li, Jiajie Yao, Shihai Xiao, and
     hashing scheme based on dynamic collision counting. In Proceedings of the 2012                 Xuecang Zhang. 2023. Diskann++: Efficient page-based search over isomor-
     ACM SIGMOD international conference on management of data. 541–552.                            phic mapped graph index using query-sensitivity entry vertex. arXiv preprint
[12] Jianyang Gao, Yutong Gou, Yuexuan Xu, Yongyi Yang, Cheng Long, and Raymond                     arXiv:2310.00402 (2023).
     Chi-Wing Wong. 2025. Practical and asymptotically optimal quantization of                 [37] OpenSearch Community. 2024. OpenSearch: Open-source enterprise-grade
     high-dimensional vectors in euclidean space for approximate nearest neighbor                   search and observability suite. https://opensearch.org/ Accessed: February 27,
     search. Proceedings of the ACM on Management of Data 3, 3 (2025), 1–26.                        2026.
[13] Jianyang Gao and Cheng Long. 2024. Rabitq: Quantizing high-dimensional                    [38] Giuseppe Ottaviano and Rossano Venturini. 2014. Partitioned elias-fano indexes.
     vectors with a theoretical error bound for approximate nearest neighbor search.                In Proceedings of the 37th international ACM SIGIR conference on Research &
     Proceedings of the ACM on Management of Data 2, 3 (2024), 1–27.                                development in information retrieval. 273–282.
[14] Goetz Graefe, Haris Volos, Hideaki Kimura, Harumi Kuno, Joseph Tucek, Mark                [39] Yun Peng, Byron Choi, Tsz Nam Chan, Jianye Yang, and Jianliang Xu. 2023.
     Lillibridge, and Alistair Veitch. 2014. In-memory performance for big data.                    Efficient approximate nearest neighbor search in multi-dimensional databases.
     Proceedings of the VLDB Endowment 8, 1 (2014), 37–48.                                          Proceedings of the ACM on Management of Data 1, 1 (2023), 1–27.
[15] Hao Guo and Youyou Lu. 2025. Achieving { Low-Latency } { Graph-Based } Vector             [40] Qdrant Technologies. 2024. Qdrant: Vector Database for the next generation of
     Search via Aligning { Best-First } Search Algorithm with { SSD } . In 19th USENIX              AI applications. https://qdrant.tech/ Accessed: February 27, 2026.
     Symposium on Operating Systems Design and Implementation (OSDI 25). 171–186.              [41] Joobo Shim, Jaewon Oh, Hongchan Roh, Jaeyoung Do, and Sang-Won Lee. 2025.
[16] Ruiqi Guo, Philip Sun, Erik Lindgren, Quan Geng, David Simcha, Felix Chern,                    Turbocharging Vector Databases using Modern SSDs. Proceedings of the VLDB
     and Sanjiv Kumar. 2020. Accelerating large-scale inference with anisotropic                    Endowment 18, 11 (2025), 4710–4722.
     vector quantization. In International Conference on Machine Learning. PMLR,               [42] Radu Stoica and Anastasia Ailamaki. 2013. Enabling efficient OS paging for main-
     3887–3896.                                                                                     memory OLTP databases. In Proceedings of the Ninth International Workshop on
[17] Guo Hao and Lu Youyou. 2026. OdinANN: Direct Insert for Consistently Sta-                      Data Management on New Hardware. 1–7.
     ble Performance in Billion-Scale Graph-Based Vector Search. In 24th USENIX                [43] Yao Tian, Xi Zhao, and Xiaofang Zhou. 2023. DB-LSH 2.0: Locality-sensitive
     Conference on File and Storage Technologies (FAST 26).                                         hashing with query-based dynamic bucketing. IEEE Transactions on Knowledge
[18] Ben Harwood and Tom Drummond. 2016. Fanng: Fast approximate nearest                            and Data Engineering 36, 3 (2023), 1000–1015.
     neighbour graphs. In Proceedings of the IEEE Conference on Computer Vision and            [44] Mengzhao Wang, Weizhi Xu, Xiaomeng Yi, Songlin Wu, Zhangyang Peng, Xi-
     Pattern Recognition. 5713–5722.                                                                angyu Ke, Yunjun Gao, Xiaoliang Xu, Rentong Guo, and Charles Xie. 2024. Star-
[19] Yongjun He, Jiacheng Lu, and Tianzheng Wang. 2020. CoroBase: coroutine-                        ling: An i/o-efficient disk-resident graph index framework for high-dimensional
     oriented main-memory database engine. arXiv preprint arXiv:2010.15981 (2020).                  vector similarity search on data segment. Proceedings of the ACM on Management
[20] Dan Hendrycks, Collin Burns, Steven Basart, Andy Zou, Mantas Mazeika, Dawn                     of Data 2, 1 (2024), 1–27.
     Song, and Jacob Steinhardt. 2020. Measuring massive multitask language under-             [45] Mengzhao Wang, Xiaoliang Xu, Qiang Yue, and Yuxiang Wang. 2021. A com-
     standing. arXiv preprint arXiv:2009.03300 (2020).                                              prehensive survey and experimental comparison of graph-based approximate
[21] Cohere Inc. 2025. Cohere Embeddings Documentation. https://docs.cohere.com/                    nearest neighbor search. arXiv preprint arXiv:2101.12631 (2021).
     docs/embeddings. Accessed: 2025-11-01.                                                    [46] Chuangxian Wei, Bin Wu, Sheng Wang, Renjie Lou, Chaoqun Zhan, Feifei Li,
[22] Piotr Indyk and Rajeev Motwani. 1998. Approximate nearest neighbors: towards                   and Yuanzhe Cai. 2020. Analyticdb-v: A hybrid analytical engine towards query
     removing the curse of dimensionality. In Proceedings of the thirtieth annual ACM               fusion for structured and unstructured data. Proceedings of the VLDB Endowment
     symposium on Theory of computing. 604–613.                                                     13, 12 (2020), 3152–3165.
[23] Suhas Jayaram Subramanya, Fnu Devvrit, Harsha Vardhan Simhadri, Ravishankar               [47] Yuming Xu, Hengyu Liang, Jin Li, Shuotao Xu, Qi Chen, Qianxi Zhang, Cheng
     Krishnawamy, and Rohan Kadekodi. 2019. Diskann: Fast accurate billion-point                    Li, Ziyue Yang, Fan Yang, Yuqing Yang, et al. 2023. Spfresh: Incremental in-place
     nearest neighbor search on a single node. Advances in neural information pro-                  update for billion-scale vector search. In Proceedings of the 29th Symposium on
     cessing Systems 32 (2019).                                                                     Operating Systems Principles. 545–561.
[24] Herve Jegou, Matthijs Douze, and Cordelia Schmid. 2010. Product quantization              [48] Peiqi Yin, Xiao Yan, Qihui Zhou, Hui Li, Xiaolu Li, Lin Zhang, Meiling Wang,
     for nearest neighbor search. IEEE transactions on pattern analysis and machine                 Xin Yao, and James Cheng. 2025. Gorgeous: Revisiting the Data Layout for
     intelligence 33, 1 (2010), 117–128.                                                            Disk-Resident High-Dimensional Vector Search. arXiv preprint arXiv:2508.15290
[25] Hervé Jégou, Romain Tavenard, Matthijs Douze, and Laurent Amsaleg. 2011.                       (2025).
     Searching in one billion vectors: re-rank with source coding. In 2011 IEEE Inter-         [49] Xi Zhao, Yao Tian, Kai Huang, Bolong Zheng, and Xiaofang Zhou. 2023. Towards
     national Conference on Acoustics, Speech and Signal Processing (ICASSP). IEEE,                 efficient index construction and approximate nearest neighbor search in high-
     861–864.                                                                                       dimensional spaces. Proceedings of the VLDB Endowment 16, 8 (2023), 1979–1991.
[26] Alfons Kemper and Donald Kossmann. 1995. Adaptable pointer swizzling strate-              [50] Xiaoyao Zhong, Haotian Li, Jiabao Jin, Mingyu Yang, Deming Chu, Xiangyu
     gies in object bases: Design, realization, and quantitative analysis. The VLDB                 Wang, Zhitao Shen, Wei Jia, George Gu, Yi Xie, et al. 2025. VSAG: An Optimized
     Journal 4, 3 (1995), 519–566.                                                                  Search Framework for Graph-based Approximate Nearest Neighbor Search. arXiv
                                                                                          13
preprint arXiv:2503.17911 (2025).




                                    14
