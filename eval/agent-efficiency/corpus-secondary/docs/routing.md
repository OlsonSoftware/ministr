# Routing

## Dispatch contract

The secondary corpus deliberately reuses the primary corpus section ID. It
describes a batch dispatcher owned by a different project and must never be
suppressed merely because the content ID collides.
