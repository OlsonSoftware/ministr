---
title: ministr_task
description: Poll a background task status. Deprecated — prefer MCP tasks/get.
---

Polls the status of a background task by id.

This tool is deprecated: prefer the standard MCP `tasks/get` mechanism instead.

{/* @generated tool-docs start — do not edit; run `npm run docs:gen` */}

> Poll a background task status. Deprecated: prefer MCP tasks/get.

## Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `task_id` | string | no | Task ID to check status for |

Annotations: read-only · idempotent.

<small>This block is generated from the live tool schema — the same definition agents receive.</small>

{/* @generated tool-docs end */}

## Returns

An MCP Task shape (camelCase fields):

- `taskId`
- `status` — one of `working`, `completed`, `failed`, `cancelled`,
  `input_required`
- `statusMessage` (optional)
- `createdAt`, `lastUpdatedAt`
- `ttl` (optional)
- `pollInterval` (optional)

## Notes

- Deprecated; use MCP `tasks/get` where available.
- Tasks expire 5 minutes after completion.
