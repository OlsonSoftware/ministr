---
title: ministr_diagnostics
description: Run the project's own toolchains and return bounded structured diagnostics.
---

Run the project's own toolchain(s) (cargo, tsc, eslint, ruff, go vet, …, plus
any SARIF-emitting tool) and return bounded, structured diagnostics — file,
range, severity, code, message — errors first, each cross-linked to the
enclosing symbol.

This is the agentic verify step: structured compiler/lint feedback as data,
never raw build logs. Language-agnostic.

{/* @generated tool-docs start — do not edit; run `npm run docs:gen` */}

> Run the project's own toolchain(s) (cargo/tsc/eslint/ruff/go vet/…, plus any SARIF-emitting tool) and return bounded STRUCTURED diagnostics (file, range, severity, code, message), errors first, each cross-linked to the enclosing symbol. The agentic verify step — structured compiler/lint feedback as data, never raw build logs. Language-agnostic.

## Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `languages` | array of string | no | Restrict to these toolchain languages (e.g. 'rust','typescript','python','go'). Omit to run every detected toolchain. |
| `limit` | integer | no | Maximum diagnostics to return. Default 100, capped at 500. |
| `project` | string | no | Optional linked-project label. Omit for primary corpus. |

Annotations: read-only · idempotent.

<small>This block is generated from the live tool schema — the same definition agents receive.</small>

{/* @generated tool-docs end */}

## Returns

A `DiagnosticsResponse` with `diagnostics` and `total`. Each diagnostic
carries file, range, severity, code, and message, cross-linked to the
enclosing symbol.

## Notes

- Errors are ordered first.
- SARIF-emitting tools are supported in addition to the built-in toolchain
  integrations.
- Responses are wrapped in `ToolResponse<T>`, carrying `coherence_alerts`,
  `indexing_in_progress`, and `next_actions`.
- Soft errors (`ok: false`) never cancel sibling parallel calls.
