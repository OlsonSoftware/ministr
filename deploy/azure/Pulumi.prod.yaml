encryptionsalt: v1:tE2UBtfbvQY=:v1:K8uBHGwM4+3AitBN:u+dYDIPCa3GQm1DLya7j+VxMQpEQrw==
config:
  ministr-azure:githubWebhookSecret:
    secure: v1:sOeWG1QU0OMF/Zzm:sCztPAqLPtBNPdVrjsqUe8LwlGgYTmgXWtUVumVX4hvKdGXIdGXEClnfJRjSXq58gmwtFw3WSIUbxt+bk6+xQfN6aGCTXwYCXOF3c0VRDKw=
  ministr-azure:projectName: ministrv2
  ministr-azure:imageTag: 37d4f68-1779275909
  ministr-azure:pgLocation: westus2
  # PHASE4 chunk 5 — right-size the indexer Job from the 8 GiB default
  # (index.ts fallback) to 4 GiB now that the cloud worker streams
  # HNSW persistence every 4 files (`cmd_indexer_worker` opts in via
  # `streaming_persist_every: Some(4)`). The cost win is ~$2/h-when-
  # running at 4 vCPU / 4 GiB vs ~$4/h at 4 vCPU / 8 GiB; the pod is
  # idle on a 0-replica KEDA scaler the rest of the time. `jobCpu`
  # stays at the default 4 — the doc hedged on dropping it without a
  # benchmark, and we don't have one yet.
  ministr-azure:jobMemory: 4Gi
