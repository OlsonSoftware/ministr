---
title: Core concepts
description: The ideas behind ministr's code intelligence — symbols, references, cross-language bridges, and hybrid search.
---

ministr's job is to understand a codebase well enough that an agent can ask real questions about it. Four ideas carry that.

## Code intelligence

| Concept | What it means |
|---|---|
| [Symbols](/docs/concepts/symbols) | ministr reads code as structure, not text — it knows a definition from a mention |
| [References](/docs/concepts/references) | A real graph of who calls, implements, and imports what — not a regex guess |
| [Cross-language bridges](/docs/concepts/bridges) | It follows a call across a language boundary — the thing grep fundamentally can't do |
| [Hybrid search](/docs/concepts/search) | Meaning-based and keyword search together, at the right level of detail |

The model stays in sync with the code on disk — ministr watches the source while it's serving and re-indexes incrementally as files change, so answers reflect the current state of the tree.
