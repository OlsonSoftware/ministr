name: App Release

on:
  push:
    tags:
      - "v*-app"

# Don't cancel in-flight app releases — tauri-action uploads
# platform-specific bundles; killing mid-upload leaves a half-
# published release draft.
concurrency:
  group: app-release-${{ github.ref }}
  cancel-in-progress: false

# tauri-action needs write to create/update the draft release.
permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-aarch64
            os: macos-latest
            args: "--target aarch64-apple-darwin"
          - platform: macos-x86_64
            os: macos-latest
            args: "--target x86_64-apple-darwin"
          - platform: windows
            os: windows-latest
            args: ""
          - platform: linux
            os: blacksmith-4vcpu-ubuntu-2404
            args: ""

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-aarch64' && 'aarch64-apple-darwin' || matrix.platform == 'macos-x86_64' && 'x86_64-apple-darwin' || '' }}

      - uses: Swatinem/rust-cache@v2

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: ministr-app/pnpm-lock.yaml

      - name: Install frontend deps
        working-directory: ministr-app
        run: pnpm install

      - name: Install Linux deps
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "ministr app ${{ github.ref_name }}"
          releaseBody: "Cross-platform ministr desktop app release."
          releaseDraft: true
          prerelease: false
          projectPath: ministr-app
          tauriScript: pnpm tauri
          args: ${{ matrix.args }}
